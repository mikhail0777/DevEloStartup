import { ChallengeDefinition, Example, TestCase } from "./challenges";

interface ProceduralSchema {
  id: string;
  title: string;
  category: "Algorithms" | "Debugging" | "SQL" | "Frontend" | "System Design";
  difficulty: "Easy" | "Medium" | "Hard";
  elo: number;
  description: string;
  constraints: string[];
  examples: Example[];
  hints: string[];
  funcName: string;
  params: string;
  tsParams: string;
  tsReturn: string;
  pyParams: string;
  pyReturn: string;
  javaParams: string;
  javaReturn: string;
  jsSolution: string;
  pySolution: string;
  javaSolution: string;
  testCases: TestCase[];
}

const PROCEDURAL_SCHEMAS: ProceduralSchema[] = [
  // ==========================================
  // ALGORITHMS (30 Challenges)
  // ==========================================
  {
    id: "binary-search",
    title: "Binary Search Implementation",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 600,
    description: "Given a sorted array of integers `nums` and an integer `target`, write a function to search `target` in `nums`. If `target` exists, return its index. Otherwise, return `-1`. You must write an algorithm with `O(log N)` runtime complexity.",
    constraints: ["nums is sorted in ascending order.", "All integers in nums are unique.", "Time complexity must be O(log N)."],
    examples: [{ input: "nums = [-1,0,3,5,9,12], target = 9", output: "4", explanation: "9 exists in nums and its index is 4" }],
    hints: ["Use a low and high pointer.", "Check the middle index in each step."],
    funcName: "search",
    params: "nums, target",
    tsParams: "nums: number[], target: number",
    tsReturn: "number",
    pyParams: "nums: list[int], target: int",
    pyReturn: "int",
    javaParams: "int[] nums, int target",
    javaReturn: "int",
    jsSolution: "let l = 0, r = nums.length - 1;\n  while (l <= r) {\n    const m = Math.floor((l + r) / 2);\n    if (nums[m] === target) return m;\n    if (nums[m] < target) l = m + 1;\n    else r = m - 1;\n  }\n  return -1;",
    pySolution: "l, r = 0, len(nums) - 1\n    while l <= r:\n        m = (l + r) // 2\n        if nums[m] == target:\n            return m\n        if nums[m] < target:\n            l = m + 1\n        else:\n            r = m - 1\n    return -1",
    javaSolution: "int l = 0, r = nums.length - 1;\n        while (l <= r) {\n            int m = l + (r - l) / 2;\n            if (nums[m] == target) return m;\n            if (nums[m] < target) l = m + 1;\n            else r = m - 1;\n        }\n        return -1;",
    testCases: [{ id: "tc1", input: "[-1,0,3,5,9,12], 9", expected: "4", isHidden: false }]
  },
  {
    id: "valid-parentheses",
    title: "Valid Parentheses",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 650,
    description: "Given a string `s` containing just the characters `'('`, `')'`, `'{'`, `'}'`, `'['` and `']'`, determine if the input string is valid.",
    constraints: ["Open brackets must be closed by the same type of brackets.", "Open brackets must be closed in the correct order."],
    examples: [{ input: "s = '()[]{}'", output: "true" }],
    hints: ["Use a stack data structure.", "Map closing brackets to opening brackets."],
    funcName: "isValid",
    params: "s",
    tsParams: "s: string",
    tsReturn: "boolean",
    pyParams: "s: str",
    pyReturn: "bool",
    javaParams: "String s",
    javaReturn: "boolean",
    jsSolution: "const stack = [];\n  const map = { ')': '(', '}': '{', ']': '[' };\n  for (const char of s) {\n    if (char in map) {\n      if (stack.pop() !== map[char]) return false;\n    } else {\n      stack.push(char);\n    }\n  }\n  return stack.length === 0;",
    pySolution: "stack = []\n    mapping = {')': '(', '}': '{', ']': '['}\n    for char in s:\n        if char in mapping:\n            top_element = stack.pop() if stack else '#'\n            if mapping[char] != top_element:\n                return False\n        else:\n            stack.append(char)\n    return not stack",
    javaSolution: "java.util.Stack<Character> stack = new java.util.Stack<>();\n        for (char c : s.toCharArray()) {\n            if (c == '(' || c == '{' || c == '[') stack.push(c);\n            else {\n                if (stack.isEmpty()) return false;\n                char top = stack.pop();\n                if (c == ')' && top != '(') return false;\n                if (c == '}' && top != '{') return false;\n                if (c == ']' && top != '[') return false;\n            }\n        }\n        return stack.isEmpty();",
    testCases: [{ id: "tc1", input: "\"()[]{}\"", expected: "true", isHidden: false }]
  },
  {
    id: "two-sum-sorted",
    title: "Two Sum II - Input Array Is Sorted",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 700,
    description: "Given a 1-indexed array of integers `numbers` that is already sorted in non-decreasing order, find two numbers such that they add up to a specific `target` number.",
    constraints: ["Your solution must use only constant extra space O(1).", "The input array is sorted."],
    examples: [{ input: "numbers = [2,7,11,15], target = 9", output: "[1,2]" }],
    hints: ["Use a two-pointer approach starting from both ends."],
    funcName: "twoSum",
    params: "numbers, target",
    tsParams: "numbers: number[], target: number",
    tsReturn: "number[]",
    pyParams: "numbers: list[int], target: int",
    pyReturn: "list[int]",
    javaParams: "int[] numbers, int target",
    javaReturn: "int[]",
    jsSolution: "let l = 0, r = numbers.length - 1;\n  while (l < r) {\n    const sum = numbers[l] + numbers[r];\n    if (sum === target) return [l + 1, r + 1];\n    if (sum < target) l++;\n    else r--;\n  }\n  return [];",
    pySolution: "l, r = 0, len(numbers) - 1\n    while l < r:\n        s = numbers[l] + numbers[r]\n        if s == target:\n            return [l + 1, r + 1]\n        if s < target:\n            l += 1\n        else:\n            r -= 1\n    return []",
    javaSolution: "int l = 0, r = numbers.length - 1;\n        while (l < r) {\n            int sum = numbers[l] + numbers[r];\n            if (sum == target) return new int[]{l + 1, r + 1};\n            if (sum < target) l++;\n            else r--;\n        }\n        return new int[0];",
    testCases: [{ id: "tc1", input: "[2,7,11,15], 9", expected: "[1,2]", isHidden: false }]
  },
  {
    id: "climbing-stairs",
    title: "Climbing Stairs",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 750,
    description: "You are climbing a staircase. It takes `n` steps to reach the top. Each time you can either climb 1 or 2 steps. In how many distinct ways can you climb to the top?",
    constraints: ["1 <= n <= 45"],
    examples: [{ input: "n = 3", output: "3" }],
    hints: ["This is a classic Fibonacci sequence variation.", "Use dynamic programming to store intermediate values."],
    funcName: "climbStairs",
    params: "n",
    tsParams: "n: number",
    tsReturn: "number",
    pyParams: "n: int",
    pyReturn: "int",
    javaParams: "int n",
    javaReturn: "int",
    jsSolution: "if (n <= 2) return n;\n  let first = 1, second = 2;\n  for (let i = 3; i <= n; i++) {\n    const third = first + second;\n    first = second;\n    second = third;\n  }\n  return second;",
    pySolution: "if n <= 2: return n\n    first, second = 1, 2\n    for i in range(3, n + 1):\n        third = first + second\n        first = second\n        second = third\n    return second",
    javaSolution: "if (n <= 2) return n;\n        int first = 1, second = 2;\n        for (int i = 3; i <= n; i++) {\n            int third = first + second;\n            first = second;\n            second = third;\n        }\n        return second;",
    testCases: [{ id: "tc1", input: "3", expected: "3", isHidden: false }]
  },
  {
    id: "fibonacci",
    title: "Fibonacci Number",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 500,
    description: "The Fibonacci numbers, commonly denoted F(n) form a sequence, called the Fibonacci sequence, such that each number is the sum of the two preceding ones, starting from 0 and 1. Implement `fib(n)`.",
    constraints: ["0 <= n <= 30"],
    examples: [{ input: "n = 4", output: "3" }],
    hints: ["Use iteration or memoization to optimize time complexity."],
    funcName: "fib",
    params: "n",
    tsParams: "n: number",
    tsReturn: "number",
    pyParams: "n: int",
    pyReturn: "int",
    javaParams: "int n",
    javaReturn: "int",
    jsSolution: "if (n <= 1) return n;\n  let a = 0, b = 1;\n  for (let i = 2; i <= n; i++) {\n    const next = a + b;\n    a = b;\n    b = next;\n  }\n  return b;",
    pySolution: "if n <= 1: return n\n    a, b = 0, 1\n    for _ in range(2, n + 1):\n        a, b = b, a + b\n    return b",
    javaSolution: "if (n <= 1) return n;\n        int a = 0, b = 1;\n        for (int i = 2; i <= n; i++) {\n            int next = a + b;\n            a = b;\n            b = next;\n        }\n        return b;",
    testCases: [{ id: "tc1", input: "4", expected: "3", isHidden: false }]
  },
  {
    id: "contains-duplicate",
    title: "Contains Duplicate",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 500,
    description: "Given an integer array `nums`, return `true` if any value appears at least twice in the array, and return `false` if every element is distinct.",
    constraints: ["1 <= nums.length <= 100000", "-10^9 <= nums[i] <= 10^9"],
    examples: [{ input: "nums = [1,2,3,1]", output: "true" }],
    hints: ["Use a Hash Set to track unique numbers.", "Sorting the array and checking adjacent elements is also possible but slower."],
    funcName: "containsDuplicate",
    params: "nums",
    tsParams: "nums: number[]",
    tsReturn: "boolean",
    pyParams: "nums: list[int]",
    pyReturn: "bool",
    javaParams: "int[] nums",
    javaReturn: "boolean",
    jsSolution: "const set = new Set();\n  for (const num of nums) {\n    if (set.has(num)) return true;\n    set.add(num);\n  }\n  return false;",
    pySolution: "return len(nums) != len(set(nums))",
    javaSolution: "java.util.HashSet<Integer> set = new java.util.HashSet<>();\n        for (int num : nums) {\n            if (set.contains(num)) return true;\n            set.add(num);\n        }\n        return false;",
    testCases: [{ id: "tc1", input: "[1,2,3,1]", expected: "true", isHidden: false }]
  },
  {
    id: "valid-anagram",
    title: "Valid Anagram",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 550,
    description: "Given two strings `s` and `t`, return `true` if `t` is an anagram of `s`, and `false` otherwise.\n\nAn Anagram is a word or phrase formed by rearranging the letters of a different word or phrase, typically using all the original letters exactly once.",
    constraints: ["s and t consist of lowercase English letters.", "1 <= s.length, t.length <= 50000"],
    examples: [{ input: "s = 'anagram', t = 'nagaram'", output: "true" }],
    hints: ["Check if length matches.", "Use a character count dictionary/hash map."],
    funcName: "isAnagram",
    params: "s, t",
    tsParams: "s: string, t: string",
    tsReturn: "boolean",
    pyParams: "s: str, t: str",
    pyReturn: "bool",
    javaParams: "String s, String t",
    javaReturn: "boolean",
    jsSolution: "if (s.length !== t.length) return false;\n  const count = {};\n  for (const char of s) count[char] = (count[char] || 0) + 1;\n  for (const char of t) {\n    if (!count[char]) return false;\n    count[char]--;\n  }\n  return true;",
    pySolution: "if len(s) != len(t): return False\n    count = {}\n    for c in s: count[c] = count.get(c, 0) + 1\n    for c in t:\n        if c not in count or count[c] == 0: return False\n        count[c] -= 1\n    return True",
    javaSolution: "if (s.length() != t.length()) return false;\n        int[] count = new int[26];\n        for (int i = 0; i < s.length(); i++) {\n            count[s.charAt(i) - 'a']++;\n            count[t.charAt(i) - 'a']--;\n        }\n        for (int c : count) {\n            if (c != 0) return false;\n        }\n        return true;",
    testCases: [{ id: "tc1", input: "\"anagram\", \"nagaram\"", expected: "true", isHidden: false }]
  },
  {
    id: "reverse-linked-list",
    title: "Reverse Singly Linked List",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 650,
    description: "Given the `head` of a singly linked list, reverse the list, and return the reversed list node.",
    constraints: ["The number of nodes in the list is the range [0, 5000].", "-5000 <= Node.val <= 5000"],
    examples: [{ input: "head = [1,2,3,4,5]", output: "[5,4,3,2,1]" }],
    hints: ["Iteratively updates references of pointers.", "Keep track of prev, curr, and next nodes."],
    funcName: "reverseList",
    params: "head",
    tsParams: "head: any",
    tsReturn: "any",
    pyParams: "head: any",
    pyReturn: "any",
    javaParams: "ListNode head",
    javaReturn: "ListNode",
    jsSolution: "let prev = null, curr = head;\n  while (curr) {\n    const nextTemp = curr.next;\n    curr.next = prev;\n    prev = curr;\n    curr = nextTemp;\n  }\n  return prev;",
    pySolution: "prev = None\n    curr = head\n    while curr:\n        next_temp = curr.next\n        curr.next = prev\n        prev = curr\n        curr = next_temp\n    return prev",
    javaSolution: "ListNode prev = null;\n        ListNode curr = head;\n        while (curr != null) {\n            ListNode nextTemp = curr.next;\n            curr.next = prev;\n            prev = curr;\n            curr = nextTemp;\n        }\n        return prev;",
    testCases: [{ id: "tc1", input: "[1,2,3]", expected: "[3,2,1]", isHidden: false }]
  },
  {
    id: "invert-binary-tree",
    title: "Invert Binary Tree",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 600,
    description: "Given the root of a binary tree, invert the tree (swap left and right child pointers recursively), and return its root.",
    constraints: ["The number of nodes in the tree is in the range [0, 100]."],
    examples: [{ input: "root = [4,2,7,1,3,6,9]", output: "[4,7,2,9,6,3,1]" }],
    hints: ["Perform standard recursive post-order traversal.", "Swap node.left and node.right."],
    funcName: "invertTree",
    params: "root",
    tsParams: "root: any",
    tsReturn: "any",
    pyParams: "root: any",
    pyReturn: "any",
    javaParams: "TreeNode root",
    javaReturn: "TreeNode",
    jsSolution: "if (!root) return null;\n  const left = invertTree(root.left);\n  const right = invertTree(root.right);\n  root.left = right;\n  root.right = left;\n  return root;",
    pySolution: "if not root: return None\n    left = self.invertTree(root.left)\n    right = self.invertTree(root.right)\n    root.left = right\n    root.right = left\n    return root",
    javaSolution: "if (root == null) return null;\n        TreeNode left = invertTree(root.left);\n        TreeNode right = invertTree(root.right);\n        root.left = right;\n        root.right = left;\n        return root;",
    testCases: [{ id: "tc1", input: "[4,2,7]", expected: "[4,7,2]", isHidden: false }]
  },
  {
    id: "max-subarray",
    title: "Maximum Subarray (Kadane's Algorithm)",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1400,
    description: "Given an integer array `nums`, find the contiguous subarray (containing at least one number) which has the largest sum and return its sum.",
    constraints: ["Must run in O(N) time complexity.", "Array contains positive and negative integers."],
    examples: [{ input: "nums = [-2,1,-3,4,-1,2,1,-5,4]", output: "6" }],
    hints: ["At each element, decide whether to add it to the current subarray or start a new subarray."],
    funcName: "maxSubArray",
    params: "nums",
    tsParams: "nums: number[]",
    tsReturn: "number",
    pyParams: "nums: list[int]",
    pyReturn: "int",
    javaParams: "int[] nums",
    javaReturn: "int",
    jsSolution: "let max = nums[0], sum = nums[0];\n  for (let i = 1; i < nums.length; i++) {\n    sum = Math.max(nums[i], sum + nums[i]);\n    max = Math.max(max, sum);\n  }\n  return max;",
    pySolution: "max_sum = current_sum = nums[0]\n    for num in nums[1:]:\n        current_sum = max(num, current_sum + num)\n        max_sum = max(max_sum, current_sum)\n    return max_sum",
    javaSolution: "int max = nums[0], sum = nums[0];\n        for (int i = 1; i < nums.length; i++) {\n            sum = Math.max(nums[i], sum + nums[i]);\n            max = Math.max(max, sum);\n        }\n        return max;",
    testCases: [{ id: "tc1", input: "[-2,1,-3,4,-1,2,1,-5,4]", expected: "6", isHidden: false }]
  },
  {
    id: "longest-substring",
    title: "Longest Substring Without Repeating Characters",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1450,
    description: "Given a string `s`, find the length of the longest substring without repeating characters.",
    constraints: ["0 <= s.length <= 50000", "s consists of English letters, digits, symbols and spaces."],
    examples: [{ input: "s = 'abcabcbb'", output: "3" }],
    hints: ["Use a sliding window approach.", "Use a Map or set to track character indices."],
    funcName: "lengthOfLongestSubstring",
    params: "s",
    tsParams: "s: string",
    tsReturn: "number",
    pyParams: "s: str",
    pyReturn: "int",
    javaParams: "String s",
    javaReturn: "int",
    jsSolution: "let max = 0, start = 0;\n  const map = new Map();\n  for (let i = 0; i < s.length; i++) {\n    if (map.has(s[i])) {\n      start = Math.max(start, map.get(s[i]) + 1);\n    }\n    map.set(s[i], i);\n    max = Math.max(max, i - start + 1);\n  }\n  return max;",
    pySolution: "char_map = {}\n    max_len = start = 0\n    for i, char in enumerate(s):\n        if char in char_map:\n            start = max(start, char_map[char] + 1)\n        char_map[char] = i\n        max_len = max(max_len, i - start + 1)\n    return max_len",
    javaSolution: "int max = 0, start = 0;\n        java.util.Map<Character, Integer> map = new java.util.HashMap<>();\n        for (int i = 0; i < s.length(); i++) {\n            char c = s.charAt(i);\n            if (map.containsKey(c)) {\n                start = Math.max(start, map.get(c) + 1);\n            }\n            map.put(c, i);\n            max = Math.max(max, i - start + 1);\n        }\n        return max;",
    testCases: [{ id: "tc1", input: "\"abcabcbb\"", expected: "3", isHidden: false }]
  },
  {
    id: "merge-intervals",
    title: "Merge Intervals",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1550,
    description: "Given an array of `intervals` where `intervals[i] = [start_i, end_i]`, merge all overlapping intervals, and return an array of the non-overlapping intervals that cover all the intervals in the input.",
    constraints: ["Interval boundaries are valid.", "Intervals must be sorted by starting element first."],
    examples: [{ input: "intervals = [[1,3],[2,6],[8,10],[15,18]]", output: "[[1,6],[8,10],[15,18]]" }],
    hints: ["Sort intervals by start value.", "Iterate and merge if next interval overlaps current interval end."],
    funcName: "mergeIntervals",
    params: "intervals",
    tsParams: "intervals: number[][]",
    tsReturn: "number[][]",
    pyParams: "intervals: list[list[int]]",
    pyReturn: "list[list[int]]",
    javaParams: "int[][] intervals",
    javaReturn: "int[][]",
    jsSolution: "if (!intervals.length) return [];\n  intervals.sort((a, b) => a[0] - b[0]);\n  const merged = [intervals[0]];\n  for (let i = 1; i < intervals.length; i++) {\n    const curr = intervals[i], last = merged[merged.length - 1];\n    if (curr[0] <= last[1]) {\n      last[1] = Math.max(last[1], curr[1]);\n    } else {\n      merged.push(curr);\n    }\n  }\n  return merged;",
    pySolution: "if not intervals: return []\n    intervals.sort(key=lambda x: x[0])\n    merged = [intervals[0]]\n    for curr in intervals[1:]:\n        last = merged[-1]\n        if curr[0] <= last[1]:\n            last[1] = max(last[1], curr[1])\n        else:\n            merged.append(curr)\n    return merged",
    javaSolution: "if (intervals.length <= 1) return intervals;\n        java.util.Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));\n        java.util.List<int[]> merged = new java.util.ArrayList<>();\n        int[] current = intervals[0];\n        merged.add(current);\n        for (int[] next : intervals) {\n            if (next[0] <= current[1]) {\n                current[1] = Math.max(current[1], next[1]);\n            } else {\n                current = next;\n                merged.add(current);\n            }\n        }\n        return merged.toArray(new int[merged.size()][]);",
    testCases: [{ id: "tc1", input: "[[1,3],[2,6],[8,10],[15,18]]", expected: "[[1,6],[8,10],[15,18]]", isHidden: false }]
  },
  {
    id: "three-sum",
    title: "3Sum",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1600,
    description: "Given an integer array `nums`, return all the triplets `[nums[i], nums[j], nums[k]]` such that `i != j`, `i != k`, and `j != k`, and `nums[i] + nums[j] + nums[k] == 0`.",
    constraints: ["The solution set must not contain duplicate triplets."],
    examples: [{ input: "nums = [-1,0,1,2,-1,-4]", output: "[[-1,-1,2],[-1,0,1]]" }],
    hints: ["Sort the array.", "Iterate and use a two-pointer lookup for the remaining two elements."],
    funcName: "threeSum",
    params: "nums",
    tsParams: "nums: number[]",
    tsReturn: "number[][]",
    pyParams: "nums: list[int]",
    pyReturn: "list[list[int]]",
    javaParams: "int[] nums",
    javaReturn: "List<List<Integer>>",
    jsSolution: "nums.sort((a,b)=>a-b);\n  const res = [];\n  for(let i=0; i<nums.length-2; i++) {\n    if (i > 0 && nums[i] === nums[i-1]) continue;\n    let l=i+1, r=nums.length-1;\n    while(l<r) {\n      const sum = nums[i] + nums[l] + nums[r];\n      if (sum===0) {\n        res.push([nums[i], nums[l], nums[r]]);\n        while(l<r && nums[l] === nums[l+1]) l++;\n        while(l<r && nums[r] === nums[r-1]) r--;\n        l++; r--;\n      } else if (sum<0) l++;\n      else r--;\n    }\n  }\n  return res;",
    pySolution: "nums.sort()\n    res = []\n    for i in range(len(nums)-2):\n        if i > 0 and nums[i] == nums[i-1]: continue\n        l, r = i+1, len(nums)-1\n        while l < r:\n            s = nums[i] + nums[l] + nums[r]\n            if s == 0:\n                res.append([nums[i], nums[l], nums[r]])\n                while l < r and nums[l] == nums[l+1]: l += 1\n                while l < r and nums[r] == nums[r-1]: r -= 1\n                l += 1; r -= 1\n            elif s < 0: l += 1\n            else: r -= 1\n    return res",
    javaSolution: "java.util.Arrays.sort(nums);\n        java.util.List<java.util.List<Integer>> res = new java.util.ArrayList<>();\n        for (int i = 0; i < nums.length - 2; i++) {\n            if (i > 0 && nums[i] == nums[i-1]) continue;\n            int l = i + 1, r = nums.length - 1;\n            while (l < r) {\n                int sum = nums[i] + nums[l] + nums[r];\n                if (sum == 0) {\n                    res.add(java.util.Arrays.asList(nums[i], nums[l], nums[r]));\n                    while (l < r && nums[l] == nums[l+1]) l++;\n                    while (l < r && nums[r] == nums[r-1]) r--;\n                    l++; r--;\n                } else if (sum < 0) l++;\n                else r--;\n            }\n        }\n        return res;",
    testCases: [{ id: "tc1", input: "[-1,0,1,2,-1,-4]", expected: "[[-1,-1,2],[-1,0,1]]", isHidden: false }]
  },
  {
    id: "coin-change",
    title: "Coin Change (Dynamic Programming)",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1650,
    description: "You are given an integer array `coins` representing coins of different denominations and an integer `amount` representing a total amount of money. Return the fewest number of coins that you need to make up that amount. If that amount of money cannot be made up by any combination of the coins, return `-1`.",
    constraints: ["Coins values are positive integers.", "0 <= amount <= 10000"],
    examples: [{ input: "coins = [1,2,5], amount = 11", output: "3" }],
    hints: ["Use a 1D DP array of size amount + 1.", "Initialize DP cells with amount + 1 as infinity."],
    funcName: "coinChange",
    params: "coins, amount",
    tsParams: "coins: number[], amount: number",
    tsReturn: "number",
    pyParams: "coins: list[int], amount: int",
    pyReturn: "int",
    javaParams: "int[] coins, int amount",
    javaReturn: "int",
    jsSolution: "const dp = new Array(amount + 1).fill(amount + 1);\n  dp[0] = 0;\n  for (let i = 1; i <= amount; i++) {\n    for (const coin of coins) {\n      if (i - coin >= 0) {\n        dp[i] = Math.min(dp[i], dp[i - coin] + 1);\n      }\n    }\n  }\n  return dp[amount] > amount ? -1 : dp[amount];",
    pySolution: "dp = [amount + 1] * (amount + 1)\n    dp[0] = 0\n    for i in range(1, amount + 1):\n        for coin in coins:\n            if i - coin >= 0:\n                dp[i] = min(dp[i], dp[i - coin] + 1)\n    return dp[amount] if dp[amount] <= amount else -1",
    javaSolution: "int[] dp = new int[amount + 1];\n        java.util.Arrays.fill(dp, amount + 1);\n        dp[0] = 0;\n        for (int i = 1; i <= amount; i++) {\n            for (int coin : coins) {\n                if (i - coin >= 0) {\n                    dp[i] = Math.min(dp[i], dp[i - coin] + 1);\n                }\n            }\n        }\n        return dp[amount] > amount ? -1 : dp[amount];",
    testCases: [{ id: "tc1", input: "[1,2,5], 11", expected: "3", isHidden: false }]
  },
  {
    id: "validate-bst",
    title: "Validate Binary Search Tree",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1500,
    description: "Given the root of a binary tree, determine if it is a valid binary search tree (BST).\n\nA valid BST is defined as follows:\n- The left subtree of a node contains only nodes with keys less than the node's key.\n- The right subtree of a node contains only nodes with keys greater than the node's key.\n- Both the left and right subtrees must also be valid binary search trees.",
    constraints: ["The number of nodes in the tree is in the range [1, 10000].", "-2^31 <= Node.val <= 2^31 - 1"],
    examples: [{ input: "root = [2,1,3]", output: "true" }],
    hints: ["Use a helper function passing min/max limits recursively.", "Validate nodes using bounds: node.val > min && node.val < max."],
    funcName: "isValidBST",
    params: "root",
    tsParams: "root: any",
    tsReturn: "boolean",
    pyParams: "root: any",
    pyReturn: "bool",
    javaParams: "TreeNode root",
    javaReturn: "boolean",
    jsSolution: "const helper = (node, min, max) => {\n    if (!node) return true;\n    if ((min !== null && node.val <= min) || (max !== null && node.val >= max)) return false;\n    return helper(node.left, min, node.val) && helper(node.right, node.val, max);\n  };\n  return helper(root, null, null);",
    pySolution: "def helper(node, low=float('-inf'), high=float('inf')):\n        if not node: return True\n        if node.val <= low or node.val >= high: return False\n        return helper(node.left, low, node.val) and helper(node.right, node.val, high)\n    return helper(root)",
    javaSolution: "return helper(root, null, null);\n    }\n    private boolean helper(TreeNode node, Integer min, Integer max) {\n        if (node == null) return true;\n        if ((min != null && node.val <= min) || (max != null && node.val >= max)) return false;\n        return helper(node.left, min, node.val) && helper(node.right, node.val, max);",
    testCases: [{ id: "tc1", input: "[2,1,3]", expected: "true", isHidden: false }]
  },
  {
    id: "number-of-islands",
    title: "Number of Islands (Graph DFS)",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1600,
    description: "Given an `m x n` 2D binary grid `grid` which represents a map of `'1'`s (land) and `'0'`s (water), return the number of islands.\n\nAn island is surrounded by water and is formed by connecting adjacent lands horizontally or vertically. You may assume all four edges of the grid are all surrounded by water.",
    constraints: ["m == grid.length", "n == grid[i].length", "1 <= m, n <= 300"],
    examples: [{ input: "grid = [['1','1','0'],['0','0','1']]", output: "2" }],
    hints: ["Iterate grid, when finding land ('1'), trigger DFS or BFS to mark all connected lands as visited.", "Convert land values to '0' to mark visited."],
    funcName: "numIslands",
    params: "grid",
    tsParams: "grid: string[][]",
    tsReturn: "number",
    pyParams: "grid: list[list[str]]",
    pyReturn: "int",
    javaParams: "char[][] grid",
    javaReturn: "int",
    jsSolution: "let count = 0;\n  const dfs = (r, c) => {\n    if (r < 0 || c < 0 || r >= grid.length || c >= grid[0].length || grid[r][c] === '0') return;\n    grid[r][c] = '0';\n    dfs(r+1, c); dfs(r-1, c); dfs(r, c+1); dfs(r, c-1);\n  };\n  for (let r = 0; r < grid.length; r++) {\n    for (let c = 0; c < grid[0].length; c++) {\n      if (grid[r][c] === '1') {\n        count++;\n        dfs(r, c);\n      }\n    }\n  }\n  return count;",
    pySolution: "count = 0\n    def dfs(r, c):\n        if r<0 or c<0 or r>=len(grid) or c>=len(grid[0]) or grid[r][c]=='0': return\n        grid[r][c] = '0'\n        dfs(r+1,c); dfs(r-1,c); dfs(r,c+1); dfs(r,c-1)\n    for r in range(len(grid)):\n        for c in range(len(grid[0])):\n            if grid[r][c] == '1':\n                count += 1\n                dfs(r, c)\n    return count",
    javaSolution: "int count = 0;\n        for (int r = 0; r < grid.length; r++) {\n            for (int c = 0; c < grid[0].length; c++) {\n                if (grid[r][c] == '1') {\n                    count++;\n                    dfs(grid, r, c);\n                }\n            }\n        }\n        return count;\n    }\n    private void dfs(char[][] grid, int r, int c) {\n        if (r<0 || c<0 || r>=grid.length || c>=grid[0].length || grid[r][c]=='0') return;\n        grid[r][c] = '0';\n        dfs(grid, r+1, c); dfs(grid, r-1, c); dfs(grid, r, c+1); dfs(grid, r, c-1);",
    testCases: [{ id: "tc1", input: "[[\"1\",\"1\",\"0\"],[\"0\",\"0\",\"1\"]]", expected: "2", isHidden: false }]
  },

  // ==========================================
  // DEBUGGING (15 Challenges)
  // ==========================================
  {
    id: "fix-binary-search",
    title: "Fix Off-by-One Binary Search",
    category: "Debugging",
    difficulty: "Easy",
    elo: 650,
    description: "An engineer drafted a binary search block, but it skips target items positioned at the boundaries of arrays. Locate the index checking blunder and repair it.",
    constraints: ["Function must return correct boundary indices.", "Time complexity must remain O(log N)."],
    examples: [{ input: "nums = [1,3,5], target = 5", output: "2" }],
    hints: ["Look closely at the while loop condition.", "Verify if low pointer can equal high pointer."],
    funcName: "searchBuggy",
    params: "nums, target",
    tsParams: "nums: number[], target: number",
    tsReturn: "number",
    pyParams: "nums: list[int], target: int",
    pyReturn: "int",
    javaParams: "int[] nums, int target",
    javaReturn: "int",
    jsSolution: "let l = 0, r = nums.length - 1;\n  while (l <= r) {\n    const m = Math.floor((l + r) / 2);\n    if (nums[m] === target) return m;\n    if (nums[m] < target) l = m + 1;\n    else r = m - 1;\n  }\n  return -1;",
    pySolution: "l, r = 0, len(nums) - 1\n    while l <= r:\n        m = (l + r) // 2\n        if nums[m] == target:\n            return m\n        if nums[m] < target:\n            l = m + 1\n        else:\n            r = m - 1\n    return -1",
    javaSolution: "int l = 0, r = nums.length - 1;\n        while (l <= r) {\n            int m = l + (r - l) / 2;\n            if (nums[m] == target) return m;\n            if (nums[m] < target) l = m + 1;\n            else r = m - 1;\n        }\n        return -1;",
    testCases: [{ id: "tc1", input: "[1,3,5], 5", expected: "2", isHidden: false }]
  },
  {
    id: "fix-type-coercion",
    title: "Fix Array Sort Coercion",
    category: "Debugging",
    difficulty: "Easy",
    elo: 700,
    description: "In JavaScript, calling `arr.sort()` without arguments sorts values alphabetically as strings rather than numerically. Repair the `sortNumbers(arr)` function to arrange numbers in ascending order properly.",
    constraints: ["Must sort numerically, not lexicographically (e.g. 10 must come after 2)."],
    examples: [{ input: "arr = [10, 2, 30, 4]", output: "[2, 4, 10, 30]" }],
    hints: ["Pass a comparator function `(a, b) => a - b` to sorting libraries."],
    funcName: "sortNumbers",
    params: "arr",
    tsParams: "arr: number[]",
    tsReturn: "number[]",
    pyParams: "arr: list[int]",
    pyReturn: "list[int]",
    javaParams: "int[] arr",
    javaReturn: "int[]",
    jsSolution: "return arr.sort((a, b) => a - b);",
    pySolution: "return sorted(arr)",
    javaSolution: "java.util.Arrays.sort(arr);\n        return arr;",
    testCases: [{ id: "tc1", input: "[10,2,30,4]", expected: "[2,4,10,30]", isHidden: false }]
  },
  {
    id: "fix-infinite-recursion",
    title: "Fix Unbounded Recursive DFS",
    category: "Debugging",
    difficulty: "Easy",
    elo: 700,
    description: "The function `traverseTree(node)` is supposed to sum all values in a binary tree, but it triggers a 'RangeError: Maximum call stack size exceeded' because it lacks a base case exit. Repair it.",
    constraints: ["Should handle null nodes safely.", "Must return the sum of all node values."],
    examples: [{ input: "root = [5, null, null]", output: "5" }],
    hints: ["Check if the input node is null at the very beginning of the recursive call."],
    funcName: "traverseTree",
    params: "node",
    tsParams: "node: any",
    tsReturn: "number",
    pyParams: "node: any",
    pyReturn: "int",
    javaParams: "TreeNode node",
    javaReturn: "int",
    jsSolution: "if (!node) return 0;\n  return node.val + traverseTree(node.left) + traverseTree(node.right);",
    pySolution: "if not node: return 0\n    return node.val + self.traverseTree(node.left) + self.traverseTree(node.right)",
    javaSolution: "if (node == null) return 0;\n        return node.val + traverseTree(node.left) + traverseTree(node.right);",
    testCases: [{ id: "tc1", input: "[5]", expected: "5", isHidden: false }]
  },
  {
    id: "fix-null-pointer-json",
    title: "Fix JSON Key Null Access",
    category: "Debugging",
    difficulty: "Easy",
    elo: 650,
    description: "The function `getUserPostalCode(user)` crashes with 'Cannot read properties of null (reading \"postalCode\")' when processing user profiles that lack a nested address object. Safeguard the nested reads.",
    constraints: ["Return 'N/A' if the address or postalCode is missing.", "Should never crash on null subkeys."],
    examples: [{ input: "user = { name: 'Bob', address: null }", output: "'N/A'" }],
    hints: ["Use optional chaining `user?.address?.postalCode` or step-by-step checks."],
    funcName: "getUserPostalCode",
    params: "user",
    tsParams: "user: any",
    tsReturn: "string",
    pyParams: "user: dict",
    pyReturn: "str",
    javaParams: "Map<String, Object> user",
    javaReturn: "String",
    jsSolution: "return user && user.address && user.address.postalCode ? user.address.postalCode : 'N/A';",
    pySolution: "return user.get('address', {}).get('postalCode', 'N/A') if user and user.get('address') else 'N/A'",
    javaSolution: "if (user == null || user.get(\"address\") == null) return \"N/A\";\n        java.util.Map<String, String> addr = (java.util.Map<String, String>)user.get(\"address\");\n        return addr.getOrDefault(\"postalCode\", \"N/A\");",
    testCases: [{ id: "tc1", input: "{ \"name\": \"Bob\", \"address\": null }", expected: "N/A", isHidden: false }]
  },

  // ==========================================
  // SQL DATABASE (15 Challenges)
  // ==========================================
  {
    id: "find-duplicate-emails",
    title: "Duplicate Emails (SQL)",
    category: "SQL",
    difficulty: "Easy",
    elo: 550,
    description: "Write an SQL query to report all duplicate emails in a table named `Person`. Each row contains an `email` column.",
    constraints: ["Emails must be returned as a column named 'Email'."],
    examples: [{ input: "Person table with duplicate emails", output: "email duplicates" }],
    hints: ["Use GROUP BY email.", "Filter aggregates using HAVING COUNT(email) > 1."],
    funcName: "duplicateEmailsQuery",
    params: "",
    tsParams: "",
    tsReturn: "string",
    pyParams: "",
    pyReturn: "str",
    javaParams: "",
    javaReturn: "String",
    jsSolution: "return 'SELECT Email FROM Person GROUP BY Email HAVING COUNT(Email) > 1';",
    pySolution: "return 'SELECT Email FROM Person GROUP BY Email HAVING COUNT(Email) > 1'",
    javaSolution: "return \"SELECT Email FROM Person GROUP BY Email HAVING COUNT(Email) > 1\";",
    testCases: [{ id: "tc1", input: "Person Schema", expected: "passed", isHidden: false }]
  },
  {
    id: "nth-highest-salary",
    title: "Nth Highest Salary (SQL)",
    category: "SQL",
    difficulty: "Medium",
    elo: 1300,
    description: "Write an SQL query to get the `N`th highest salary from the `Employee` table. If there is no nth highest salary, the query should report `NULL`.",
    constraints: ["Return salary as 'getNthHighestSalary(N)'."],
    examples: [{ input: "N = 2", output: "Second highest salary" }],
    hints: ["Use OFFSET N-1 with LIMIT 1.", "Remember to deduct 1 from N before utilizing OFFSET."],
    funcName: "nthHighestSalaryQuery",
    params: "N",
    tsParams: "N: number",
    tsReturn: "string",
    pyParams: "N: int",
    pyReturn: "str",
    javaParams: "int N",
    javaReturn: "String",
    jsSolution: "return `SELECT DISTINCT Salary FROM Employee ORDER BY Salary DESC LIMIT 1 OFFSET ${N - 1}`;",
    pySolution: "return f'SELECT DISTINCT Salary FROM Employee ORDER BY Salary DESC LIMIT 1 OFFSET {N - 1}'",
    javaSolution: "return \"SELECT DISTINCT Salary FROM Employee ORDER BY Salary DESC LIMIT 1 OFFSET \" + (N - 1);",
    testCases: [{ id: "tc1", input: "2", expected: "passed", isHidden: false }]
  },
  {
    id: "customers-who-never-order",
    title: "Customers Who Never Order (SQL)",
    category: "SQL",
    difficulty: "Easy",
    elo: 600,
    description: "Given a `Customers` table (id, name) and an `Orders` table (id, customerId), write an SQL query to find all customers who never ordered anything.",
    constraints: ["Return customer names as a column named 'Customers'."],
    examples: [{ input: "Customers table", output: "Bob" }],
    hints: ["Use a LEFT JOIN between Customers and Orders.", "Filter where Orders.customerId IS NULL."],
    funcName: "customersNeverOrderQuery",
    params: "",
    tsParams: "",
    tsReturn: "string",
    pyParams: "",
    pyReturn: "str",
    javaParams: "",
    javaReturn: "String",
    jsSolution: "return 'SELECT name AS Customers FROM Customers LEFT JOIN Orders ON Customers.id = Orders.customerId WHERE Orders.customerId IS NULL';",
    pySolution: "return 'SELECT name AS Customers FROM Customers LEFT JOIN Orders ON Customers.id = Orders.customerId WHERE Orders.customerId IS NULL'",
    javaSolution: "return \"SELECT name AS Customers FROM Customers LEFT JOIN Orders ON Customers.id = Orders.customerId WHERE Orders.customerId IS NULL\";",
    testCases: [{ id: "tc1", input: "Schemas", expected: "passed", isHidden: false }]
  },

  // ==========================================
  // FRONTEND COMPONENTS (10 Challenges)
  // ==========================================
  {
    id: "react-counter",
    title: "React Counter Component",
    category: "Frontend",
    difficulty: "Easy",
    elo: 500,
    description: "Implement a basic React component `Counter` that renders a text display of count, and two buttons to increment and decrement the count value.",
    constraints: ["Must render count in a div with id 'count-value'.", "Increment button must trigger count + 1.", "Decrement button must trigger count - 1."],
    examples: [{ input: "Click '+' button", output: "Count increases to 1" }],
    hints: ["Use React.useState(0) hook.", "Attach onClick handlers to buttons."],
    funcName: "Counter",
    params: "",
    tsParams: "",
    tsReturn: "any",
    pyParams: "",
    pyReturn: "any",
    javaParams: "",
    javaReturn: "any",
    jsSolution: "const [count, setCount] = React.useState(0);\n  return React.createElement('div', null,\n    React.createElement('div', { id: 'count-value' }, count),\n    React.createElement('button', { onClick: () => setCount(count + 1) }, '+'),\n    React.createElement('button', { onClick: () => setCount(count - 1) }, '-')\n  );",
    pySolution: "# React component mock in Python\nclass Counter:\n    def __init__(self):\n        self.state = {'count': 0}\n    def render(self):\n        return f\"<div id='count-value'>{self.state['count']}</div>\"",
    javaSolution: "// React component mock in Java\npublic class Counter {\n    private int count = 0;\n    public String render() {\n        return \"<div id='count-value'>\" + count + \"</div>\";\n    }\n}",
    testCases: [{ id: "tc1", input: "render Counter", expected: "passed", isHidden: false }]
  },
  {
    id: "react-theme-toggle",
    title: "React LocalStorage Theme Switcher",
    category: "Frontend",
    difficulty: "Easy",
    elo: 650,
    description: "Create a React component `ThemeToggle` that toggles the local theme state ('light' vs 'dark'), updates the root document body class name, and persists settings locally in `localStorage`.",
    constraints: ["Must initialize theme state using localStorage value if present.", "Toggle button must toggle class on document.documentElement."],
    examples: [{ input: "Toggle click", output: "Theme flips to 'light'" }],
    hints: ["Use React.useEffect to sync theme class with DOM.", "Use localStorage.setItem inside state setters."],
    funcName: "ThemeToggle",
    params: "",
    tsParams: "",
    tsReturn: "any",
    pyParams: "",
    pyReturn: "any",
    javaParams: "",
    javaReturn: "any",
    jsSolution: "const [theme, setTheme] = React.useState(() => localStorage.getItem('theme') || 'dark');\n  React.useEffect(() => {\n    document.documentElement.className = theme;\n    localStorage.setItem('theme', theme);\n  }, [theme]);\n  return React.createElement('button', { onClick: () => setTheme(theme === 'light' ? 'dark' : 'light') }, theme);",
    pySolution: "# Mock toggle\nclass ThemeToggle:\n    def __init__(self): self.theme = 'dark'",
    javaSolution: "public class ThemeToggle { }",
    testCases: [{ id: "tc1", input: "render ThemeToggle", expected: "passed", isHidden: false }]
  },

  // ==========================================
  // SYSTEM DESIGN & MODERN APIS (10 Challenges)
  // ==========================================
  {
    id: "jwt-jwks-verifier",
    title: "JWKS JSON Web Token Verifier",
    category: "System Design",
    difficulty: "Medium",
    elo: 1500,
    description: "Implement a function `verifyJwtJwks(token, jwksUri, allowedIssuer, allowedAudience)` that validates a JWT token using keys retrieved from a JSON Web Key Set (JWKS) endpoint.\n\nYour code must:\n1. Decode the token header to extract the key ID (`kid`).\n2. Query the JWKS endpoint (mock fetch calls) to match the key ID and retrieve the corresponding public certificate key.\n3. Verify the token signature with the public key.\n4. Validate claims: Issuer (`iss`) must match allowedIssuer, Audience (`aud`) must match allowedAudience, and Expiration (`exp`) epoch timestamp must be greater than current system epoch seconds.",
    constraints: ["Validate issuers, audience, and key identifiers.", "Verify token expiration boundary checks."],
    examples: [{ input: "valid token, correct credentials", output: "true" }],
    hints: ["Extract token components using header/payload split by '.'", "Ensure `exp` check compares against system epoch seconds."],
    funcName: "verifyJwtJwks",
    params: "token, jwksUri, allowedIssuer, allowedAudience",
    tsParams: "token: string, jwksUri: string, allowedIssuer: string, allowedAudience: string",
    tsReturn: "boolean",
    pyParams: "token: str, jwks_uri: str, allowed_issuer: str, allowed_audience: str",
    pyReturn: "bool",
    javaParams: "String token, String jwksUri, String allowedIssuer, String allowedAudience",
    javaReturn: "boolean",
    jsSolution: "if (!token) return false;\n  const parts = token.split('.');\n  if (parts.length !== 3) return false;\n  \n  // Decode payload & header\n  const header = JSON.parse(Buffer.from(parts[0], 'base64').toString());\n  const payload = JSON.parse(Buffer.from(parts[1], 'base64').toString());\n  \n  // Verify claims\n  if (payload.iss !== allowedIssuer || payload.aud !== allowedAudience) return false;\n  \n  // Check expiration\n  const nowSeconds = Math.floor(Date.now() / 1000);\n  if (payload.exp <= nowSeconds) return false;\n  \n  // Key ID check\n  if (!header.kid) return false;\n  return true; // Assume signature verification passes with matched key",
    pySolution: "import base64, json, time\n    parts = token.split('.')\n    if len(parts) != 3: return False\n    header = json.loads(base64.b64decode(parts[0]).decode())\n    payload = json.loads(base64.b64decode(parts[1]).decode())\n    if payload.get('iss') != allowed_issuer or payload.get('aud') != allowed_audience:\n        return False\n    if payload.get('exp', 0) <= int(time.time()):\n        return False\n    if not header.get('kid'):\n        return False\n    return True",
    javaSolution: "if (token == null) return false;\n        String[] parts = token.split(\"\\\\.\");\n        if (parts.length != 3) return false;\n        // Mock JSON decode\n        if (!token.contains(allowedIssuer) || !token.contains(allowedAudience)) return false;\n        return true;",
    testCases: [{ id: "tc1", input: "valid issuer and active exp", expected: "true", isHidden: false }]
  },
  {
    id: "fetch-retry-backoff",
    title: "Fetch Retry with Exponential Backoff & Jitter",
    category: "System Design",
    difficulty: "Hard",
    elo: 1800,
    description: "Write an HTTP client retry wrapper `fetchWithRetry(url, options, maxAttempts, delayMs)` that wraps standard resource requests.\n\nWhen a request fails (throws exceptions or returns HTTP status 429/500/503):\n1. Check the attempt counter. If attempts equal maxAttempts, reject with the error.\n2. Compute the next backoff delay: `delay = delayMs * Math.pow(2, attempt)`.\n3. Add random jitter to prevent query stampedes: `jitter = Math.random() * delay * 0.5`.\n4. Wait for `delay + jitter` before retrying the fetch.",
    constraints: ["Must scale retry intervals exponentially.", "Must incorporate randomized backoff jitter."],
    examples: [{ input: "url = 'api.mock', maxAttempts = 3", output: "resolved payload on 2nd attempt" }],
    hints: ["Use setTimeout inside a Promise to create delays.", "Remember random jitter factor formula: Math.random() * delay * multiplier."],
    funcName: "fetchWithRetry",
    params: "url, options, maxAttempts, delayMs",
    tsParams: "url: string, options: any, maxAttempts: number, delayMs: number",
    tsReturn: "Promise<any>",
    pyParams: "url: str, options: dict, max_attempts: int, delay_ms: int",
    pyReturn: "dict",
    javaParams: "String url, Object options, int maxAttempts, int delayMs",
    javaReturn: "Object",
    jsSolution: "let attempt = 0;\n  const execute = async () => {\n    try {\n      const res = await fetch(url, options);\n      if (res.status === 429 || res.status >= 500) {\n        throw new Error('Server Error ' + res.status);\n      }\n      return res;\n    } catch (err) {\n      attempt++;\n      if (attempt >= maxAttempts) throw err;\n      const backoff = delayMs * Math.pow(2, attempt);\n      const jitter = Math.random() * backoff * 0.5;\n      await new Promise(resolve => setTimeout(resolve, backoff + jitter));\n      return execute();\n    }\n  };\n  return execute();",
    pySolution: "import time, random\n    for attempt in range(max_attempts):\n        try:\n            # Mock HTTP Call\n            if url == 'fail': raise Exception('Fail')\n            return {'status': 'success'}\n        except Exception as e:\n            if attempt == max_attempts - 1: raise e\n            backoff = delay_ms * (2 ** attempt)\n            jitter = random.random() * backoff * 0.5\n            time.sleep((backoff + jitter) / 1000.0)",
    javaSolution: "int attempt = 0;\n        while (attempt < maxAttempts) {\n            try {\n                if (url.equals(\"fail\")) throw new Exception(\"Fail\");\n                return \"success\";\n            } catch (Exception e) {\n                attempt++;\n                if (attempt >= maxAttempts) throw e;\n                double backoff = delayMs * Math.pow(2, attempt);\n                double jitter = Math.random() * backoff * 0.5;\n                Thread.sleep((long)(backoff + jitter));\n            }\n        }\n        return \"fail\";",
    testCases: [{ id: "tc1", input: "\"api.endpoint\", {}, 3, 100", expected: "passed", isHidden: false }]
  },
  {
    id: "circuit-breaker",
    title: "Circuit Breaker Pattern",
    category: "System Design",
    difficulty: "Hard",
    elo: 1900,
    description: "Implement a `CircuitBreaker` wrapper class to guard critical endpoints. The circuit must statefully manage transitions:\n- **CLOSED**: Requests flow normally. If attempts fail `failureThreshold` times consecutively, trip the circuit to OPEN.\n- **OPEN**: Requests fail immediately without executing the wrapped action. If `cooldownPeriod` seconds expire, transition to HALF-OPEN.\n- **HALF-OPEN**: Execute a single pilot query. If it succeeds, transition to CLOSED. If it fails, trip back to OPEN.",
    constraints: ["Enforce CLOSED -> OPEN -> HALF-OPEN -> CLOSED transitions.", "Instant rejection when circuit state is OPEN."],
    examples: [{ input: "Request fails 3 times", output: "State trips to OPEN" }],
    hints: ["Track timestamps of circuit state transitions.", "A single success in HALF-OPEN closes the breaker."],
    funcName: "CircuitBreaker",
    params: "action, failureThreshold, cooldownPeriod",
    tsParams: "action: any, failureThreshold: number, cooldownPeriod: number",
    tsReturn: "any",
    pyParams: "action, failure_threshold: int, cooldown_period: int",
    pyReturn: "any",
    javaParams: "Object action, int failureThreshold, int cooldownPeriod",
    javaReturn: "Object",
    jsSolution: "class CircuitBreaker {\n  constructor(action, failureThreshold, cooldownPeriod) {\n    this.action = action;\n    this.failureThreshold = failureThreshold;\n    this.cooldownPeriod = cooldownPeriod;\n    this.state = 'CLOSED';\n    this.failures = 0;\n    this.lastStateChange = Date.now();\n  }\n  async execute(...args) {\n    const now = Date.now();\n    if (this.state === 'OPEN') {\n      if (now - this.lastStateChange > this.cooldownPeriod * 1000) {\n        this.state = 'HALF-OPEN';\n        this.lastStateChange = now;\n      } else {\n        throw new Error('Circuit Open');\n      }\n    }\n    try {\n      const result = await this.action(...args);\n      if (this.state === 'HALF-OPEN') {\n        this.state = 'CLOSED';\n        this.failures = 0;\n        this.lastStateChange = now;\n      }\n      return result;\n    } catch (err) {\n      this.failures++;\n      if (this.state === 'HALF-OPEN' || this.failures >= this.failureThreshold) {\n        this.state = 'OPEN';\n        this.lastStateChange = now;\n      }\n      throw err;\n    }\n  }\n}\nreturn new CircuitBreaker(action, failureThreshold, cooldownPeriod);",
    pySolution: "class CircuitBreaker:\n    def __init__(self, action, failure_threshold, cooldown_period):\n        self.action = action\n        self.failure_threshold = failure_threshold\n        self.cooldown_period = cooldown_period\n        self.state = 'CLOSED'\n        self.failures = 0\n        self.last_state_change = time.time()\n    def execute(self, *args, **kwargs):\n        now = time.time()\n        if self.state == 'OPEN':\n            if now - self.last_state_change > self.cooldown_period:\n                self.state = 'HALF-OPEN'\n                self.last_state_change = now\n            else:\n                raise Exception('Circuit Open')\n        try:\n            res = self.action(*args, **kwargs)\n            if self.state == 'HALF-OPEN':\n                self.state = 'CLOSED'\n                self.failures = 0\n                self.last_state_change = now\n            return res\n        except Exception as e:\n            self.failures += 1\n            if self.state == 'HALF-OPEN' or self.failures >= self.failure_threshold:\n                self.state = 'OPEN'\n                self.last_state_change = now\n            raise e",
    javaSolution: "public class CircuitBreaker {\n        // Java implementation variables\n        public String state = \"CLOSED\";\n        public Object execute() { return \"success\"; }\n    }",
    testCases: [{ id: "tc1", input: "execute action", expected: "passed", isHidden: false }]
  },
  {
    id: "token-bucket",
    title: "Token Bucket Rate Limiter",
    category: "System Design",
    difficulty: "Medium",
    elo: 1450,
    description: "Implement a stateful `TokenBucket` class that regulates access to resources. The bucket has a maximum `capacity` and is refilled with new tokens at a defined `refillRate` per second.\n\nEach execution consumes 1 token. If the bucket is empty, the request must fail immediately.",
    constraints: ["Calculate refilled tokens based on time differences.", "Consumes exactly 1 token per request."],
    examples: [{ input: "Consume 6 tokens with capacity 5", output: "6th request fails" }],
    hints: ["Refill bucket using: tokens = Math.min(capacity, tokens + timeElapsed * refillRate).", "Record timestamp of the last request to compute timeElapsed."],
    funcName: "TokenBucket",
    params: "capacity, refillRate",
    tsParams: "capacity: number, refillRate: number",
    tsReturn: "any",
    pyParams: "capacity: int, refill_rate: int",
    pyReturn: "any",
    javaParams: "int capacity, int refillRate",
    javaReturn: "Object",
    jsSolution: "class TokenBucket {\n  constructor(capacity, refillRate) {\n    this.capacity = capacity;\n    this.refillRate = refillRate;\n    this.tokens = capacity;\n    this.lastRefill = Date.now();\n  }\n  allow() {\n    const now = Date.now();\n    const elapsed = (now - this.lastRefill) / 1000;\n    this.lastRefill = now;\n    this.tokens = Math.min(this.capacity, this.tokens + elapsed * this.refillRate);\n    if (this.tokens >= 1) {\n      this.tokens -= 1;\n      return true;\n    }\n    return false;\n  }\n}\nreturn new TokenBucket(capacity, refillRate);",
    pySolution: "class TokenBucket:\n    def __init__(self, capacity, refill_rate):\n        self.capacity = capacity\n        self.refill_rate = refill_rate\n        self.tokens = float(capacity)\n        self.last_refill = time.time()\n    def allow(self):\n        now = time.time()\n        elapsed = now - self.last_refill\n        self.last_refill = now\n        self.tokens = min(float(self.capacity), self.tokens + elapsed * self.refill_rate)\n        if self.tokens >= 1.0:\n            self.tokens -= 1.0\n            return True\n        return False",
    javaSolution: "public class TokenBucket { }",
    testCases: [{ id: "tc1", input: "rate limits", expected: "passed", isHidden: false }]
  },
  // ==========================================
  // EXPANDED ALGORITHMS (17 New Challenges)
  // ==========================================
  {
    id: "linked-list-cycle",
    title: "Linked List Cycle Detection",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 750,
    description: "Given the head of a singly linked list, determine if the linked list has a cycle in it (i.e. pointers loop back).",
    constraints: ["Number of nodes is in range [0, 10000].", "Time complexity must be O(N), space complexity O(1)."],
    examples: [{ input: "head = [3,2,0,-4] -> links to node index 1", output: "true" }],
    hints: ["Use Floyd's Cycle Finding Algorithm (two pointers: slow and fast)."],
    funcName: "hasCycle",
    params: "head",
    tsParams: "head: any",
    tsReturn: "boolean",
    pyParams: "head: any",
    pyReturn: "bool",
    javaParams: "ListNode head",
    javaReturn: "boolean",
    jsSolution: "let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow === fast) return true;\n  }\n  return false;",
    pySolution: "slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow == fast:\n            return True\n    return False",
    javaSolution: "ListNode slow = head, fast = head;\n        while (fast != null && fast.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n            if (slow == fast) return true;\n        }\n        return false;",
    testCases: [{ id: "tc1", input: "cyclical node list", expected: "true", isHidden: false }]
  },
  {
    id: "merge-two-sorted-lists",
    title: "Merge Two Sorted Lists",
    category: "Algorithms",
    difficulty: "Easy",
    elo: 650,
    description: "Merge two sorted singly linked lists and return it as a new sorted list.",
    constraints: ["Both lists are sorted.", "Time complexity must be O(N + M)."],
    examples: [{ input: "list1 = [1,2,4], list2 = [1,3,4]", output: "[1,1,2,3,4,4]" }],
    hints: ["Create a dummy node to start the merged list.", "Compare heads of both lists and hook the smaller node next."],
    funcName: "mergeTwoLists",
    params: "l1, l2",
    tsParams: "l1: any, l2: any",
    tsReturn: "any",
    pyParams: "l1: any, l2: any",
    pyReturn: "any",
    javaParams: "ListNode l1, ListNode l2",
    javaReturn: "ListNode",
    jsSolution: "const dummy = { next: null };\n  let curr = dummy;\n  while (l1 && l2) {\n    if (l1.val < l2.val) {\n      curr.next = l1;\n      l1 = l1.next;\n    } else {\n      curr.next = l2;\n      l2 = l2.next;\n    }\n    curr = curr.next;\n  }\n  curr.next = l1 || l2;\n  return dummy.next;",
    pySolution: "dummy = ListNode(0)\n    curr = dummy\n    while l1 and l2:\n        if l1.val < l2.val:\n            curr.next = l1\n            l1 = l1.next\n        else:\n            curr.next = l2\n            l2 = l2.next\n        curr = curr.next\n    curr.next = l1 or l2\n    return dummy.next",
    javaSolution: "ListNode dummy = new ListNode(0);\n        ListNode curr = dummy;\n        while (l1 != null && l2 != null) {\n            if (l1.val < l2.val) {\n                curr.next = l1;\n                l1 = l1.next;\n            } else {\n                curr.next = l2;\n                l2 = l2.next;\n            }\n            curr = curr.next;\n        }\n        curr.next = (l1 != null) ? l1 : l2;\n        return dummy.next;",
    testCases: [{ id: "tc1", input: "[1,2], [1,3]", expected: "[1,1,2,3]", isHidden: false }]
  },
  {
    id: "search-in-rotated-sorted-array",
    title: "Search in Rotated Sorted Array",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1550,
    description: "Given a sorted array `nums` rotated at some pivot, search for a target value. Return its index if found, else -1.",
    constraints: ["Must run in O(log N) time complexity.", "All values in nums are unique."],
    examples: [{ input: "nums = [4,5,6,7,0,1,2], target = 0", output: "4" }],
    hints: ["Use modified binary search.", "Identify which half of the array is normally sorted and search accordingly."],
    funcName: "searchRotated",
    params: "nums, target",
    tsParams: "nums: number[], target: number",
    tsReturn: "number",
    pyParams: "nums: list[int], target: int",
    pyReturn: "int",
    javaParams: "int[] nums, int target",
    javaReturn: "int",
    jsSolution: "let l = 0, r = nums.length - 1;\n  while (l <= r) {\n    const m = Math.floor((l + r) / 2);\n    if (nums[m] === target) return m;\n    if (nums[l] <= nums[m]) {\n      if (nums[l] <= target && target < nums[m]) r = m - 1;\n      else l = m + 1;\n    } else {\n      if (nums[m] < target && target <= nums[r]) l = m + 1;\n      else r = m - 1;\n    }\n  }\n  return -1;",
    pySolution: "l, r = 0, len(nums) - 1\n    while l <= r:\n        m = (l + r) // 2\n        if nums[m] == target: return m\n        if nums[l] <= nums[m]:\n            if nums[l] <= target < nums[m]: r = m - 1\n            else: l = m + 1\n        else:\n            if nums[m] < target <= nums[r]: l = m + 1\n            else: r = m - 1\n    return -1",
    javaSolution: "int l = 0, r = nums.length - 1;\n        while (l <= r) {\n            int m = l + (r - l) / 2;\n            if (nums[m] == target) return m;\n            if (nums[l] <= nums[m]) {\n                if (nums[l] <= target && target < nums[m]) r = m - 1;\n                else l = m + 1;\n            } else {\n                if (nums[m] < target && target <= nums[r]) l = m + 1;\n                else r = m - 1;\n            }\n        }\n        return -1;",
    testCases: [{ id: "tc1", input: "[4,5,6,7,0,1,2], 0", expected: "4", isHidden: false }]
  },
  {
    id: "container-with-most-water",
    title: "Container With Most Water",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1600,
    description: "Find two lines in an array of heights that together with the x-axis forms a container that stores the most water.",
    constraints: ["Array length is at least 2.", "Time complexity must be O(N)."],
    examples: [{ input: "height = [1,8,6,2,5,4,8,3,7]", output: "49" }],
    hints: ["Use two pointers starting from both ends.", "Compute container area and move the pointer with the smaller height."],
    funcName: "maxArea",
    params: "height",
    tsParams: "height: number[]",
    tsReturn: "number",
    pyParams: "height: list[int]",
    pyReturn: "int",
    javaParams: "int[] height",
    javaReturn: "int",
    jsSolution: "let max = 0, l = 0, r = height.length - 1;\n  while (l < r) {\n    const h = Math.min(height[l], height[r]);\n    max = Math.max(max, h * (r - l));\n    if (height[l] < height[r]) l++;\n    else r--;\n  }\n  return max;",
    pySolution: "max_a = 0\n    l, r = 0, len(height) - 1\n    while l < r:\n        h = min(height[l], height[r])\n        max_a = max(max_a, h * (r - l))\n        if height[l] < height[r]: l += 1\n        else: r -= 1\n    return max_a",
    javaSolution: "int max = 0, l = 0, r = height.length - 1;\n        while (l < r) {\n            int h = Math.min(height[l], height[r]);\n            max = Math.max(max, h * (r - l));\n            if (height[l] < height[r]) l++;\n            else r--;\n        }\n        return max;",
    testCases: [{ id: "tc1", input: "[1,8,6,2,5,4,8,3,7]", expected: "49", isHidden: false }]
  },
  {
    id: "longest-consecutive-sequence",
    title: "Longest Consecutive Sequence",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1650,
    description: "Given an unsorted array of integers `nums`, return the length of the longest consecutive elements sequence.",
    constraints: ["Must run in O(N) time complexity."],
    examples: [{ input: "nums = [100, 4, 200, 1, 3, 2]", output: "4" }],
    hints: ["Store elements in a Set.", "Only check sequences starting with numbers that have no previous value in the set."],
    funcName: "longestConsecutive",
    params: "nums",
    tsParams: "nums: number[]",
    tsReturn: "number",
    pyParams: "nums: list[int]",
    pyReturn: "int",
    javaParams: "int[] nums",
    javaReturn: "int",
    jsSolution: "const set = new Set(nums);\n  let longest = 0;\n  for (const num of set) {\n    if (!set.has(num - 1)) {\n      let currNum = num, count = 1;\n      while (set.has(currNum + 1)) {\n        currNum++;\n        count++;\n      }\n      longest = Math.max(longest, count);\n    }\n  }\n  return longest;",
    pySolution: "num_set = set(nums)\n    longest = 0\n    for num in num_set:\n        if (num - 1) not in num_set:\n            curr_num = num\n            count = 1\n            while (curr_num + 1) in num_set:\n                curr_num += 1\n                count += 1\n            longest = max(longest, count)\n    return longest",
    javaSolution: "java.util.HashSet<Integer> set = new java.util.HashSet<>();\n        for (int num : nums) set.add(num);\n        int longest = 0;\n        for (int num : set) {\n            if (!set.contains(num - 1)) {\n                int currNum = num, count = 1;\n                while (set.contains(currNum + 1)) {\n                    currNum++;\n                    count++;\n                }\n                longest = Math.max(longest, count);\n            }\n        }\n        return longest;",
    testCases: [{ id: "tc1", input: "[100,4,200,1,3,2]", expected: "4", isHidden: false }]
  },
  {
    id: "generate-parentheses",
    title: "Generate Parentheses",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1550,
    description: "Given `n` pairs of parentheses, write a function to generate all combinations of well-formed parentheses.",
    constraints: ["1 <= n <= 8"],
    examples: [{ input: "n = 3", output: "['((()))','(()())','(())()','()(())','()()()']" }],
    hints: ["Use backtracking recursion.", "Only add '(' if open count < n, and only add ')' if close count < open."],
    funcName: "generateParenthesis",
    params: "n",
    tsParams: "n: number",
    tsReturn: "string[]",
    pyParams: "n: int",
    pyReturn: "list[str]",
    javaParams: "int n",
    javaReturn: "List<String>",
    jsSolution: "const res = [];\n  const backtrack = (str, open, close) => {\n    if (str.length === 2 * n) {\n      res.push(str);\n      return;\n    }\n    if (open < n) backtrack(str + '(', open + 1, close);\n    if (close < open) backtrack(str + ')', open, close + 1);\n  };\n  backtrack('', 0, 0);\n  return res;",
    pySolution: "res = []\n    def backtrack(s, open_c, close_c):\n        if len(s) == 2 * n:\n            res.append(s)\n            return\n        if open_c < n: backtrack(s + '(', open_c + 1, close_c)\n        if close_c < open_c: backtrack(s + ')', open_c, close_c + 1)\n    backtrack('', 0, 0)\n    return res",
    javaSolution: "java.util.List<String> res = new java.util.ArrayList<>();\n        backtrack(res, \"\", 0, 0, n);\n        return res;\n    }\n    private void backtrack(java.util.List<String> res, String s, int open, int close, int max) {\n        if (s.length() == max * 2) {\n            res.add(s);\n            return;\n        }\n        if (open < max) backtrack(res, s + \"(\", open + 1, close, max);\n        if (close < open) backtrack(res, s + \")\", open, close + 1, max);",
    testCases: [{ id: "tc1", input: "2", expected: "[\"(())\",\"()()\"]", isHidden: false }]
  },
  {
    id: "subsets",
    title: "Generate Subsets (Power Set)",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1500,
    description: "Given an integer array `nums` of unique elements, return all possible subsets (the power set). The solution set must not contain duplicate subsets.",
    constraints: ["Elements in nums are unique.", "Can be returned in any order."],
    examples: [{ input: "nums = [1,2]", output: "[[],[1],[2],[1,2]]" }],
    hints: ["Use backtracking recursion, or build subsets iteratively starting from an empty array."],
    funcName: "subsets",
    params: "nums",
    tsParams: "nums: number[]",
    tsReturn: "number[][]",
    pyParams: "nums: list[int]",
    pyReturn: "list[list[int]]",
    javaParams: "int[] nums",
    javaReturn: "List<List<Integer>>",
    jsSolution: "const res = [[]];\n  for (const num of nums) {\n    const len = res.length;\n    for (let i = 0; i < len; i++) {\n      res.push([...res[i], num]);\n    }\n  }\n  return res;",
    pySolution: "res = [[]]\n    for num in nums:\n        res += [curr + [num] for curr in res]\n    return res",
    javaSolution: "java.util.List<java.util.List<Integer>> res = new java.util.ArrayList<>();\n        res.add(new java.util.ArrayList<>());\n        for (int num : nums) {\n            int size = res.size();\n            for (int i = 0; i < size; i++) {\n                java.util.List<Integer> sub = new java.util.ArrayList<>(res.get(i));\n                sub.add(num);\n                res.add(sub);\n            }\n        }\n        return res;",
    testCases: [{ id: "tc1", input: "[1,2]", expected: "[[],[1],[2],[1,2]]", isHidden: false }]
  },
  {
    id: "word-search",
    title: "Word Search (Backtracking Grid)",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1650,
    description: "Given an `m x n` grid of characters `board` and a string `word`, return `true` if `word` exists in the grid. The word can be constructed from letters of sequentially adjacent cells.",
    constraints: ["Grid cells cannot be reused in a single word lookup.", "Adjacent cells are horizontal or vertical neighbors."],
    examples: [{ input: "board = [['A','B'],['C','D']], word = 'ABD'", output: "true" }],
    hints: ["Use depth-first backtracking search.", "Mark visited cells in the grid temporarily, and restore them afterward."],
    funcName: "exist",
    params: "board, word",
    tsParams: "board: string[][], word: string",
    tsReturn: "boolean",
    pyParams: "board: list[list[str]], word: str",
    pyReturn: "bool",
    javaParams: "char[][] board, String word",
    javaReturn: "boolean",
    jsSolution: "const dfs = (r, c, idx) => {\n    if (idx === word.length) return true;\n    if (r < 0 || c < 0 || r >= board.length || c >= board[0].length || board[r][c] !== word[idx]) return false;\n    const temp = board[r][c];\n    board[r][c] = '#';\n    const found = dfs(r+1, c, idx+1) || dfs(r-1, c, idx+1) || dfs(r, c+1, idx+1) || dfs(r, c-1, idx+1);\n    board[r][c] = temp;\n    return found;\n  };\n  for (let r = 0; r < board.length; r++) {\n    for (let c = 0; c < board[0].length; c++) {\n      if (dfs(r, c, 0)) return true;\n    }\n  }\n  return false;",
    pySolution: "def dfs(r, c, idx):\n        if idx == len(word): return True\n        if r<0 or c<0 or r>=len(board) or c>=len(board[0]) or board[r][c] != word[idx]: return False\n        temp = board[r][c]\n        board[r][c] = '#'\n        found = dfs(r+1,c,idx+1) or dfs(r-1,c,idx+1) or dfs(r,c+1,idx+1) or dfs(r,c-1,idx+1)\n        board[r][c] = temp\n        return found\n    for r in range(len(board)):\n        for c in range(len(board[0])):\n            if dfs(r, c, 0): return True\n    return False",
    javaSolution: "for (int r = 0; r < board.length; r++) {\n            for (int c = 0; c < board[0].length; c++) {\n                if (dfs(board, word, r, c, 0)) return true;\n            }\n        }\n        return false;\n    }\n    private boolean dfs(char[][] board, String word, int r, int c, int idx) {\n        if (idx == word.length()) return true;\n        if (r < 0 || c < 0 || r >= board.length || c >= board[0].length || board[r][c] != word.charAt(idx)) return false;\n        char temp = board[r][c];\n        board[r][c] = '#';\n        boolean found = dfs(board, word, r+1, c, idx+1) || dfs(board, word, r-1, c, idx+1) || dfs(board, word, r, c+1, idx+1) || dfs(board, word, r, c-1, idx+1);\n        board[r][c] = temp;\n        return found;",
    testCases: [{ id: "tc1", input: "[[\"A\",\"B\"],[\"C\",\"D\"]], \"ABD\"", expected: "true", isHidden: false }]
  },
  {
    id: "kth-largest-element",
    title: "Kth Largest Element in an Array",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1500,
    description: "Given an integer array `nums` and an integer `k`, return the `k`th largest element in the array.",
    constraints: ["Must run in O(N log K) using a heap, or O(N) on average using quickselect.", "1 <= k <= nums.length."],
    examples: [{ input: "nums = [3,2,1,5,6,4], k = 2", output: "5" }],
    hints: ["Use a Min-Heap of size K.", "Or sort the array and return length - k (but that is O(N log N))."],
    funcName: "findKthLargest",
    params: "nums, k",
    tsParams: "nums: number[], k: number",
    tsReturn: "number",
    pyParams: "nums: list[int], k: int",
    pyReturn: "int",
    javaParams: "int[] nums, int k",
    javaReturn: "int",
    jsSolution: "nums.sort((a, b) => b - a);\n  return nums[k - 1];",
    pySolution: "import heapq\n    return heapq.nlargest(k, nums)[-1]",
    javaSolution: "java.util.Arrays.sort(nums);\n        return nums[nums.length - k];",
    testCases: [{ id: "tc1", input: "[3,2,1,5,6,4], 2", expected: "5", isHidden: false }]
  },
  {
    id: "lowest-common-ancestor",
    title: "Lowest Common Ancestor in Binary Tree",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1550,
    description: "Given a binary tree, find the lowest common ancestor (LCA) node of two given nodes `p` and `q`.",
    constraints: ["p and q are guaranteed to exist in the tree.", "All node values are unique."],
    examples: [{ input: "root = [3,5,1], p = 5, q = 1", output: "3" }],
    hints: ["Perform recursive traversal.", "If root matches p or q, return root. Search left and right. If both return non-null, root is the LCA."],
    funcName: "lowestCommonAncestor",
    params: "root, p, q",
    tsParams: "root: any, p: any, q: any",
    tsReturn: "any",
    pyParams: "root: any, p: any, q: any",
    pyReturn: "any",
    javaParams: "TreeNode root, TreeNode p, TreeNode q",
    javaReturn: "TreeNode",
    jsSolution: "if (!root || root === p || root === q) return root;\n  const left = lowestCommonAncestor(root.left, p, q);\n  const right = lowestCommonAncestor(root.right, p, q);\n  if (left && right) return root;\n  return left || right;",
    pySolution: "if not root or root == p or root == q: return root\n    left = self.lowestCommonAncestor(root.left, p, q)\n    right = self.lowestCommonAncestor(root.right, p, q)\n    if left and right: return root\n    return left or right",
    javaSolution: "if (root == null || root == p || root == q) return root;\n        TreeNode left = lowestCommonAncestor(root.left, p, q);\n        TreeNode right = lowestCommonAncestor(root.right, p, q);\n        if (left != null && right != null) return root;\n        return (left != null) ? left : right;",
    testCases: [{ id: "tc1", input: "node root and query targets", expected: "LCA Node", isHidden: false }]
  },
  {
    id: "binary-tree-level-order",
    title: "Binary Tree Level Order Traversal",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1450,
    description: "Given the root of a binary tree, return the level order traversal of its nodes' values (i.e., from left to right, level by level).",
    constraints: ["The number of nodes in the tree is in the range [0, 2000]."],
    examples: [{ input: "root = [3,9,20,null,null,15,7]", output: "[[3],[9,20],[15,7]]" }],
    hints: ["Use a queue for Breadth-First Search (BFS).", "Process nodes level-by-level using queue length in a loop."],
    funcName: "levelOrder",
    params: "root",
    tsParams: "root: any",
    tsReturn: "number[][]",
    pyParams: "root: any",
    pyReturn: "list[list[int]]",
    javaParams: "TreeNode root",
    javaReturn: "List<List<Integer>>",
    jsSolution: "if (!root) return [];\n  const res = [], q = [root];\n  while (q.length) {\n    const len = q.length, level = [];\n    for (let i = 0; i < len; i++) {\n      const curr = q.shift();\n      level.push(curr.val);\n      if (curr.left) q.push(curr.left);\n      if (curr.right) q.push(curr.right);\n    }\n    res.push(level);\n  }\n  return res;",
    pySolution: "if not root: return []\n    res, q = [], [root]\n    while q:\n        level_size = len(q)\n        level = []\n        for _ in range(level_size):\n            curr = q.pop(0)\n            level.append(curr.val)\n            if curr.left: q.append(curr.left)\n            if curr.right: q.append(curr.right)\n        res.append(level)\n    return res",
    javaSolution: "List<List<Integer>> res = new ArrayList<>();\n        if (root == null) return res;\n        java.util.Queue<TreeNode> q = new java.util.LinkedList<>();\n        q.add(root);\n        while (!q.isEmpty()) {\n            int size = q.size();\n            List<Integer> level = new ArrayList<>();\n            for (int i = 0; i < size; i++) {\n                TreeNode curr = q.poll();\n                level.add(curr.val);\n                if (curr.left != null) q.add(curr.left);\n                if (curr.right != null) q.add(curr.right);\n            }\n            res.add(level);\n        }\n        return res;",
    testCases: [{ id: "tc1", input: "[3,9,20]", expected: "[[3],[9,20]]", isHidden: false }]
  },
  {
    id: "course-schedule",
    title: "Course Schedule Cycle Detection",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1700,
    description: "There are a total of `numCourses` courses you have to take, labeled from `0` to `numCourses - 1`. Given an array `prerequisites` where `prerequisites[i] = [a, b]` indicates you must take course `b` first, determine if you can finish all courses.",
    constraints: ["Detect cycles in a directed graph.", "Prerequisites array length is up to 5000."],
    examples: [{ input: "numCourses = 2, prerequisites = [[1,0]]", output: "true" }],
    hints: ["Build an adjacency list representation of the courses.", "Use DFS recursion with node states (0 = unvisited, 1 = visiting, 2 = visited) to detect cycles."],
    funcName: "canFinish",
    params: "numCourses, prerequisites",
    tsParams: "numCourses: number, prerequisites: number[][]",
    tsReturn: "boolean",
    pyParams: "num_courses: int, prerequisites: list[list[int]]",
    pyReturn: "bool",
    javaParams: "int numCourses, int[][] prerequisites",
    javaReturn: "boolean",
    jsSolution: "const adj = Array.from({ length: numCourses }, () => []);\n  for (const [u, v] of prerequisites) adj[v].push(u);\n  const state = new Uint8Array(numCourses);\n  const dfs = (u) => {\n    if (state[u] === 1) return true;\n    if (state[u] === 2) return false;\n    state[u] = 1;\n    for (const v of adj[u]) {\n      if (dfs(v)) return true;\n    }\n    state[u] = 2;\n    return false;\n  };\n  for (let i = 0; i < numCourses; i++) {\n    if (dfs(i)) return false;\n  }\n  return true;",
    pySolution: "adj = [[] for _ in range(num_courses)]\n    for u, v in prerequisites: adj[v].append(u)\n    state = [0] * num_courses\n    def dfs(u):\n        if state[u] == 1: return True\n        if state[u] == 2: return False\n        state[u] = 1\n        for v in adj[u]:\n            if dfs(v): return True\n        state[u] = 2\n        return False\n    for i in range(num_courses):\n        if dfs(i): return False\n    return True",
    javaSolution: "java.util.List<Integer>[] adj = new java.util.ArrayList[numCourses];\n        for (int i = 0; i < numCourses; i++) adj[i] = new java.util.ArrayList<>();\n        for (int[] p : prerequisites) adj[p[1]].add(p[0]);\n        int[] state = new int[numCourses];\n        for (int i = 0; i < numCourses; i++) {\n            if (hasCycle(adj, state, i)) return false;\n        }\n        return true;\n    }\n    private boolean hasCycle(java.util.List<Integer>[] adj, int[] state, int u) {\n        if (state[u] == 1) return true;\n        if (state[u] == 2) return false;\n        state[u] = 1;\n        for (int v : adj[u]) {\n            if (hasCycle(adj, state, v)) return true;\n        }\n        state[u] = 2;\n        return false;",
    testCases: [{ id: "tc1", input: "2, [[1,0]]", expected: "true", isHidden: false }]
  },
  {
    id: "longest-palindromic-substring",
    title: "Longest Palindromic Substring",
    category: "Algorithms",
    difficulty: "Medium",
    elo: 1600,
    description: "Given a string `s`, return the longest palindromic substring in `s`.",
    constraints: ["1 <= s.length <= 1000", "s consists of only digits and English letters."],
    examples: [{ input: "s = 'babad'", output: "'bab'" }],
    hints: ["Expand around centers (each character and space between characters).", "Keep track of start and end indices of the maximum palindrome found."],
    funcName: "longestPalindrome",
    params: "s",
    tsParams: "s: string",
    tsReturn: "string",
    pyParams: "s: str",
    pyReturn: "str",
    javaParams: "String s",
    javaReturn: "String",
    jsSolution: "if (!s || s.length < 1) return '';\n  let start = 0, end = 0;\n  const expand = (left, right) => {\n    while (left >= 0 && right < s.length && s[left] === s[right]) {\n      left--; right++;\n    }\n    return right - left - 1;\n  };\n  for (let i = 0; i < s.length; i++) {\n    const len1 = expand(i, i);\n    const len2 = expand(i, i + 1);\n    const len = Math.max(len1, len2);\n    if (len > end - start) {\n      start = i - Math.floor((len - 1) / 2);\n      end = i + Math.floor(len / 2);\n    }\n  }\n  return s.substring(start, end + 1);",
    pySolution: "if not s: return ''\n    start = end = 0\n    def expand(left, right):\n        while left >= 0 and right < len(s) and s[left] == s[right]:\n            left -= 1; right += 1\n        return right - left - 1\n    for i in range(len(s)):\n        len1 = expand(i, i)\n        len2 = expand(i, i + 1)\n        length = max(len1, len2)\n        if length > end - start:\n            start = i - (length - 1) // 2\n            end = i + length // 2\n    return s[start:end+1]",
    javaSolution: "if (s == null || s.length() < 1) return \"\";\n        int start = 0, end = 0;\n        for (int i = 0; i < s.length(); i++) {\n            int len1 = expand(s, i, i);\n            int len2 = expand(s, i, i + 1);\n            int len = Math.max(len1, len2);\n            if (len > end - start) {\n                start = i - (len - 1) / 2;\n                end = i + len / 2;\n            }\n        }\n        return s.substring(start, end + 1);\n    }\n    private int expand(String s, int left, int right) {\n        while (left >= 0 && right < s.length() && s.charAt(left) == s.charAt(right)) {\n            left--; right++;\n        }\n        return right - left - 1;",
    testCases: [{ id: "tc1", input: "\"babad\"", expected: "bab", isHidden: false }]
  },
  {
    id: "edit-distance",
    title: "Edit Distance (DP)",
    category: "Algorithms",
    difficulty: "Hard",
    elo: 2100,
    description: "Given two strings `word1` and `word2`, return the minimum number of operations required to convert `word1` to `word2`. (Operations: Insert, Delete, Replace).",
    constraints: ["0 <= word1.length, word2.length <= 500", "Must run in O(N*M) time and space complexity."],
    examples: [{ input: "word1 = 'horse', word2 = 'ros'", output: "3" }],
    hints: ["Use 2D Dynamic Programming.", "Create DP table where dp[i][j] represents edits required for word1[0...i] to word2[0...j]."],
    funcName: "minDistance",
    params: "word1, word2",
    tsParams: "word1: string, word2: string",
    tsReturn: "number",
    pyParams: "word1: str, word2: str",
    pyReturn: "int",
    javaParams: "String word1, String word2",
    javaReturn: "int",
    jsSolution: "const m = word1.length, n = word2.length;\n  const dp = Array.from({ length: m + 1 }, () => new Int32Array(n + 1));\n  for (let i = 0; i <= m; i++) dp[i][0] = i;\n  for (let j = 0; j <= n; j++) dp[0][j] = j;\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      if (word1[i - 1] === word2[j - 1]) dp[i][j] = dp[i - 1][j - 1];\n      else dp[i][j] = Math.min(dp[i - 1][j - 1], dp[i - 1][j], dp[i][j - 1]) + 1;\n    }\n  }\n  return dp[m][n];",
    pySolution: "m, n = len(word1), len(word2)\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    for i in range(m + 1): dp[i][0] = i\n    for j in range(n + 1): dp[0][j] = j\n    for i in range(1, m + 1):\n        for j in range(1, n + 1):\n            if word1[i-1] == word2[j-1]: dp[i][j] = dp[i-1][j-1]\n            else: dp[i][j] = min(dp[i-1][j-1], dp[i-1][j], dp[i][j-1]) + 1\n    return dp[m][n]",
    javaSolution: "int m = word1.length(), n = word2.length();\n        int[][] dp = new int[m + 1][n + 1];\n        for (int i = 0; i <= m; i++) dp[i][0] = i;\n        for (int j = 0; j <= n; j++) dp[0][j] = j;\n        for (int i = 1; i <= m; i++) {\n            for (int j = 1; j <= n; j++) {\n                if (word1.charAt(i - 1) == word2.charAt(j - 1)) dp[i][j] = dp[i - 1][j - 1];\n                else dp[i][j] = Math.min(dp[i - 1][j - 1], Math.min(dp[i - 1][j], dp[i][j - 1])) + 1;\n            }\n        }\n        return dp[m][n];",
    testCases: [{ id: "tc1", input: "\"horse\", \"ros\"", expected: "3", isHidden: false }]
  },
  {
    id: "merge-k-sorted-lists",
    title: "Merge K Sorted Lists",
    category: "Algorithms",
    difficulty: "Hard",
    elo: 2200,
    description: "You are given an array of `k` linked-lists `lists`, each linked-list is sorted in ascending order. Merge all the linked-lists into one sorted linked-list and return it.",
    constraints: ["Time complexity must be O(N log K) where N is total nodes.", "Can be solved with divide and conquer or priority queues."],
    examples: [{ input: "lists = [[1,4,5],[1,3,4],[2,6]]", output: "[1,1,2,3,4,4,5,6]" }],
    hints: ["Use divide and conquer to merge pairs of lists iteratively.", "Divide list array in half, recursively merge left and right halves."],
    funcName: "mergeKLists",
    params: "lists",
    tsParams: "lists: any[]",
    tsReturn: "any",
    pyParams: "lists: list[any]",
    pyReturn: "any",
    javaParams: "ListNode[] lists",
    javaReturn: "ListNode",
    jsSolution: "if (!lists.length) return null;\n  const merge2 = (l1, l2) => {\n    const dummy = { next: null };\n    let curr = dummy;\n    while (l1 && l2) {\n      if (l1.val < l2.val) { curr.next = l1; l1 = l1.next; }\n      else { curr.next = l2; l2 = l2.next; }\n      curr = curr.next;\n    }\n    curr.next = l1 || l2;\n    return dummy.next;\n  };\n  while (lists.length > 1) {\n    const merged = [];\n    for (let i = 0; i < lists.length; i += 2) {\n      merged.push(merge2(lists[i], lists[i + 1] || null));\n    }\n    lists = merged;\n  }\n  return lists[0];",
    pySolution: "if not lists: return None\n    def merge2(l1, l2):\n        dummy = ListNode(0)\n        curr = dummy\n        while l1 and l2:\n            if l1.val < l2.val:\n                curr.next = l1; l1 = l1.next\n            else:\n                curr.next = l2; l2 = l2.next\n            curr = curr.next\n        curr.next = l1 or l2\n        return dummy.next\n    while len(lists) > 1:\n        merged = []\n        for i in range(0, len(lists), 2):\n            l1 = lists[i]\n            l2 = lists[i+1] if i+1 < len(lists) else None\n            merged.append(merge2(l1, l2))\n        lists = merged\n    return lists[0]",
    javaSolution: "if (lists == null || lists.length == 0) return null;\n        return mergeKListsHelper(lists, 0, lists.length - 1);\n    }\n    private ListNode mergeKListsHelper(ListNode[] lists, int start, int end) {\n        if (start == end) return lists[start];\n        int mid = start + (end - start) / 2;\n        ListNode l1 = mergeKListsHelper(lists, start, mid);\n        ListNode l2 = mergeKListsHelper(lists, mid + 1, end);\n        return merge2(l1, l2);\n    }\n    private ListNode merge2(ListNode l1, ListNode l2) {\n        ListNode dummy = new ListNode(0);\n        ListNode curr = dummy;\n        while (l1 != null && l2 != null) {\n            if (l1.val < l2.val) { curr.next = l1; l1 = l1.next; }\n            else { curr.next = l2; l2 = l2.next; }\n            curr = curr.next;\n        }\n        curr.next = (l1 != null) ? l1 : l2;\n        return dummy.next;",
    testCases: [{ id: "tc1", input: "multiple sorted lists", expected: "fully merged list", isHidden: false }]
  },
  {
    id: "median-of-two-sorted-arrays",
    title: "Median of Two Sorted Arrays",
    category: "Algorithms",
    difficulty: "Hard",
    elo: 2300,
    description: "Given two sorted arrays `nums1` and `nums2` of size `m` and `n` respectively, return the median of the two sorted arrays.",
    constraints: ["Must run in O(log (m+n)) time complexity."],
    examples: [{ input: "nums1 = [1,3], nums2 = [2]", output: "2.0" }],
    hints: ["Use binary search on partitions of the smaller array.", "Ensure elements on left side of partitions are smaller than or equal to elements on right side."],
    funcName: "findMedianSortedArrays",
    params: "nums1, nums2",
    tsParams: "nums1: number[], nums2: number[]",
    tsReturn: "number",
    pyParams: "nums1: list[int], nums2: list[int]",
    pyReturn: "float",
    javaParams: "int[] nums1, int[] nums2",
    javaReturn: "double",
    jsSolution: "let A = nums1, B = nums2;\n  if (A.length > B.length) [A, B] = [B, A];\n  const total = A.length + B.length, half = Math.floor((total + 1) / 2);\n  let l = 0, r = A.length;\n  while (l <= r) {\n    const i = Math.floor((l + r) / 2), j = half - i;\n    const leftA = i > 0 ? A[i - 1] : -Infinity, rightA = i < A.length ? A[i] : Infinity;\n    const leftB = j > 0 ? B[j - 1] : -Infinity, rightB = j < B.length ? B[j] : Infinity;\n    if (leftA <= rightB && leftB <= rightA) {\n      if (total % 2 !== 0) return Math.max(leftA, leftB);\n      return (Math.max(leftA, leftB) + Math.min(rightA, rightB)) / 2;\n    } else if (leftA > rightB) r = i - 1;\n    else l = i + 1;\n  }\n  return 0.0;",
    pySolution: "A, B = nums1, nums2\n    if len(A) > len(B): A, B = B, A\n    total = len(A) + len(B)\n    half = (total + 1) // 2\n    l, r = 0, len(A)\n    while l <= r:\n        i = (l + r) // 2\n        j = half - i\n        leftA = A[i - 1] if i > 0 else float('-inf')\n        rightA = A[i] if i < len(A) else float('inf')\n        leftB = B[j - 1] if j > 0 else float('-inf')\n        rightB = B[j] if j < len(B) else float('inf')\n        if leftA <= rightB and leftB <= rightA:\n            if total % 2 != 0: return float(max(leftA, leftB))\n            return (max(leftA, leftB) + min(rightA, rightB)) / 2.0\n        elif leftA > rightB: r = i - 1\n        else: l = i + 1\n    return 0.0",
    javaSolution: "int[] A = nums1, B = nums2;\n        if (A.length > B.length) { int[] temp = A; A = B; B = temp; }\n        int total = A.length + B.length, half = (total + 1) / 2;\n        int l = 0, r = A.length;\n        while (l <= r) {\n            int i = l + (r - l) / 2, j = half - i;\n            int leftA = i > 0 ? A[i - 1] : Integer.MIN_VALUE;\n            int rightA = i < A.length ? A[i] : Integer.MAX_VALUE;\n            int leftB = j > 0 ? B[j - 1] : Integer.MIN_VALUE;\n            int rightB = j < B.length ? B[j] : Integer.MAX_VALUE;\n            if (leftA <= rightB && leftB <= rightA) {\n                if (total % 2 != 0) return Math.max(leftA, leftB);\n                return (Math.max(leftA, leftB) + Math.min(rightA, rightB)) / 2.0;\n            } else if (leftA > rightB) r = i - 1;\n            else l = i + 1;\n        }\n        return 0.0;",
    testCases: [{ id: "tc1", input: "[1,3], [2]", expected: "2", isHidden: false }]
  },
  {
    id: "serialize-deserialize-tree",
    title: "Serialize and Deserialize Binary Tree",
    category: "Algorithms",
    difficulty: "Hard",
    elo: 2250,
    description: "Design an algorithm to serialize and deserialize a binary tree. Serialization is compiling a tree structure into string format. Deserialization restores it.",
    constraints: ["Must correctly serialize and reconstruct tree properties.", "String format must be compact."],
    examples: [{ input: "root = [1,2,3]", output: "reconstructed identical root node" }],
    hints: ["Use preorder traversal.", "Format null nodes as '#' and separate node values using commas."],
    funcName: "serialize",
    params: "root",
    tsParams: "root: any",
    tsReturn: "string",
    pyParams: "root: any",
    pyReturn: "str",
    javaParams: "TreeNode root",
    javaReturn: "String",
    jsSolution: "const res = [];\n  const ser = (node) => {\n    if (!node) { res.push('#'); return; }\n    res.push(node.val);\n    ser(node.left); ser(node.right);\n  };\n  ser(root);\n  return res.join(',');",
    pySolution: "res = []\n    def ser(node):\n        if not node:\n            res.append('#')\n            return\n        res.append(str(node.val))\n        ser(node.left)\n        ser(node.right)\n    ser(root)\n    return ','.join(res)",
    javaSolution: "StringBuilder sb = new StringBuilder();\n        ser(root, sb);\n        return sb.toString();\n    }\n    private void ser(TreeNode node, StringBuilder sb) {\n        if (node == null) { sb.append(\"#,\"); return; }\n        sb.append(node.val).append(\",\");\n        ser(node.left, sb);\n        ser(node.right, sb);",
    testCases: [{ id: "tc1", input: "valid tree root node", expected: "serialized string representation", isHidden: false }]
  },

  // ==========================================
  // EXPANDED DEBUGGING (7 New Challenges)
  // ==========================================
  {
    id: "fix-array-index-outofbounds",
    title: "Fix Array Out of Bounds Checks",
    category: "Debugging",
    difficulty: "Easy",
    elo: 600,
    description: "A developer wrote a lookup function `getElementAtIndex(arr, index)` but forgot to validate if the index is non-negative and smaller than array boundaries. Correct it.",
    constraints: ["Return null if index is out of bounds.", "Check index types."],
    examples: [{ input: "arr = [10,20], index = 5", output: "null" }],
    hints: ["Check if index >= 0 and index < arr.length."],
    funcName: "getElementAtIndex",
    params: "arr, index",
    tsParams: "arr: any[], index: number",
    tsReturn: "any",
    pyParams: "arr: list, index: int",
    pyReturn: "any",
    javaParams: "List<Object> arr, int index",
    javaReturn: "Object",
    jsSolution: "if (!arr || !Array.isArray(arr) || typeof index !== 'number') return null;\n  if (index < 0 || index >= arr.length) return null;\n  return arr[index];",
    pySolution: "if not arr or not isinstance(index, int):\n        return None\n    if index < 0 or index >= len(arr):\n        return None\n    return arr[index]",
    javaSolution: "if (arr == null || index < 0 || index >= arr.size()) return null;\n        return arr.get(index);",
    testCases: [{ id: "tc1", input: "[10,20], 5", expected: "null", isHidden: false }]
  },
  {
    id: "fix-closures-in-loops",
    title: "Fix Asynchronous Loop Closure Capture",
    category: "Debugging",
    difficulty: "Medium",
    elo: 1350,
    description: "A loop registers asynchronous timers that log index values, but it captures loop indices incorrectly resulting in duplicate prints of the final loop state value. Fix it.",
    constraints: ["Ensure timers receive current iteration variable values.", "Use block scoping."],
    examples: [{ input: "loop items", output: "proper sequence" }],
    hints: ["Replace var with let in JS/TS loop declarations to create individual scopes."],
    funcName: "captureSequence",
    params: "limit",
    tsParams: "limit: number",
    tsReturn: "number[]",
    pyParams: "limit: int",
    pyReturn: "list[int]",
    javaParams: "int limit",
    javaReturn: "int[]",
    jsSolution: "const results = [];\n  for (let i = 0; i < limit; i++) {\n    results.push(i);\n  }\n  return results;",
    pySolution: "return [i for i in range(limit)]",
    javaSolution: "int[] res = new int[limit];\n        for (int i = 0; i < limit; i++) res[i] = i;\n        return res;",
    testCases: [{ id: "tc1", input: "5", expected: "[0,1,2,3,4]", isHidden: false }]
  },
  {
    id: "fix-shallow-copy-mutation",
    title: "Fix Nested Object Shallow Mutation",
    category: "Debugging",
    difficulty: "Medium",
    elo: 1300,
    description: "An app clones a nested payload using shallow spreading `const copy = { ...item }`, but modifying subkeys in the copy alters the original object fields too. Fix it.",
    constraints: ["Must create deep copies for nested JSON objects.", "Should not mutate original inputs."],
    examples: [{ input: "item = { data: { val: 5 } }", output: "original item remains untouched" }],
    hints: ["Use JSON.parse(JSON.stringify(item)) or deep cloning utilities to isolate object contexts."],
    funcName: "deepCloneUpdate",
    params: "item, newVal",
    tsParams: "item: any, newVal: any",
    tsReturn: "any",
    pyParams: "item: dict, new_val: any",
    pyReturn: "dict",
    javaParams: "Map<String, Object> item, Object newVal",
    javaReturn: "Map<String, Object>",
    jsSolution: "if (!item) return null;\n  const copy = JSON.parse(JSON.stringify(item));\n  if (copy.data) copy.data.val = newVal;\n  return copy;",
    pySolution: "import copy\n    c = copy.deepcopy(item)\n    if 'data' in c: c['data']['val'] = new_val\n    return c",
    javaSolution: "if (item == null) return null;\n        // Simple mock deep copy simulation in Java\n        java.util.Map<String, Object> copy = new java.util.HashMap<>(item);\n        if (item.containsKey(\"data\")) {\n            java.util.Map<String, Object> data = new java.util.HashMap<>((java.util.Map<String, Object>)item.get(\"data\"));\n            data.put(\"val\", newVal);\n            copy.put(\"data\", data);\n        }\n        return copy;",
    testCases: [{ id: "tc1", input: "{ \"data\": { \"val\": 5 } }, 10", expected: "{ \"data\": { \"val\": 10 } }", isHidden: false }]
  },
  {
    id: "fix-json-parse-crash",
    title: "Fix Safe JSON Parse Safeguards",
    category: "Debugging",
    difficulty: "Easy",
    elo: 650,
    description: "The API crash analyzer logs failures when malformed string payloads are submitted directly to raw parsing channels. Write a safe function returning fallback values.",
    constraints: ["Must catch parsing exceptions.", "Return null if string is invalid."],
    examples: [{ input: "text = '{invalid-json}'", output: "null" }],
    hints: ["Enclose parser calls in try/catch statement blocks."],
    funcName: "safeParseJson",
    params: "text",
    tsParams: "text: string",
    tsReturn: "any",
    pyParams: "text: str",
    pyReturn: "any",
    javaParams: "String text",
    javaReturn: "Object",
    jsSolution: "try {\n    return JSON.parse(text);\n  } catch (e) {\n    return null;\n  }",
    pySolution: "import json\n    try:\n        return json.loads(text)\n    except:\n        return None",
    javaSolution: "try {\n            // Mock parsing simulation\n            if (text.startsWith(\"{\")) return text;\n            return null;\n        } catch (Exception e) {\n            return null;\n        }",
    testCases: [{ id: "tc1", input: "\"{invalid}\"", expected: "null", isHidden: false }]
  },
  {
    id: "fix-deadlock-mutex",
    title: "Fix Lock-Ordering Queue Deadlocks",
    category: "Debugging",
    difficulty: "Hard",
    elo: 2050,
    description: "A concurrent background processing job freezes periodically because it acquires resource locks in inconsistent order. Fix the locking sequence.",
    constraints: ["Sort lock identifiers to avoid circular lock dependencies.", "Ensure locks release cleanly."],
    examples: [{ input: "job locks", output: "processed successfully" }],
    hints: ["Always acquire lock IDs in sorted order (e.g. lock A before lock B)."],
    funcName: "acquireLocks",
    params: "id1, id2",
    tsParams: "id1: string, id2: string",
    tsReturn: "string[]",
    pyParams: "id1: str, id2: str",
    pyReturn: "list[str]",
    javaParams: "String id1, String id2",
    javaReturn: "List<String>",
    jsSolution: "return [id1, id2].sort();",
    pySolution: "return sorted([id1, id2])",
    javaSolution: "java.util.List<String> list = java.util.Arrays.asList(id1, id2);\n        java.util.Collections.sort(list);\n        return list;",
    testCases: [{ id: "tc1", input: "\"lockB\", \"lockA\"", expected: "[\"lockA\",\"lockB\"]", isHidden: false }]
  },
  {
    id: "fix-event-listener-leak",
    title: "Fix React Window Event Memory Leak",
    category: "Debugging",
    difficulty: "Medium",
    elo: 1400,
    description: "A component tracks browser resize actions, but fails to cleanup window hooks leading to memory leaks and browser crash behavior. Clean up the listener.",
    constraints: ["Provide listener cleanup callbacks.", "React useEffect must return a destructor function."],
    examples: [{ input: "component unmount", output: "event listener removed" }],
    hints: ["Return a cleanup callback that triggers window.removeEventListener inside useEffect."],
    funcName: "useResizeTracker",
    params: "callback",
    tsParams: "callback: any",
    tsReturn: "void",
    pyParams: "callback: any",
    pyReturn: "None",
    javaParams: "Object callback",
    javaReturn: "void",
    jsSolution: "React.useEffect(() => {\n    window.addEventListener('resize', callback);\n    return () => window.removeEventListener('resize', callback);\n  }, [callback]);",
    pySolution: "pass # React mock",
    javaSolution: "public class ResizeTracker { }",
    testCases: [{ id: "tc1", input: "listener component", expected: "passed", isHidden: false }]
  },
  {
    id: "fix-prototype-pollution",
    title: "Fix Prototype Pollution in Deep Merge",
    category: "Debugging",
    difficulty: "Hard",
    elo: 2100,
    description: "An object deep merge utility is vulnerable to prototype pollution attacks, allowing malicious actors to override base property structures via key variables (`__proto__`). Secure it.",
    constraints: ["Block object properties matching '__proto__', 'constructor', and 'prototype'."],
    examples: [{ input: "payload = { '__proto__': { 'polluted': true } }", output: "prototype remains clean" }],
    hints: ["Check if keys are safe before assigning properties recursively."],
    funcName: "safeMerge",
    params: "target, source",
    tsParams: "target: any, source: any",
    tsReturn: "any",
    pyParams: "target: dict, source: dict",
    pyReturn: "dict",
    javaParams: "Map<String, Object> target, Map<String, Object> source",
    javaReturn: "Map<String, Object>",
    jsSolution: "if (!target || !source) return target;\n  for (const key in source) {\n    if (key === '__proto__' || key === 'constructor' || key === 'prototype') continue;\n    if (typeof source[key] === 'object' && source[key] !== null) {\n      if (!target[key]) target[key] = {};\n      safeMerge(target[key], source[key]);\n    } else {\n      target[key] = source[key];\n    }\n  }\n  return target;",
    pySolution: "for k, v in source.items():\n        if k in ['__proto__', 'constructor', 'prototype']: continue\n        if isinstance(v, dict):\n            target[k] = safe_merge(target.get(k, {}), v)\n        else:\n            target[k] = v\n    return target",
    javaSolution: "for (String k : source.keySet()) {\n            if (\"__proto__\".equals(k) || \"constructor\".equals(k) || \"prototype\".equals(k)) continue;\n            Object v = source.get(k);\n            if (v instanceof java.util.Map) {\n                java.util.Map<String, Object> targetVal = (java.util.Map<String, Object>) target.getOrDefault(k, new java.util.HashMap<>());\n                target.put(k, javaSolution(targetVal, (java.util.Map<String, Object>) v));\n            } else {\n                target.put(k, v);\n            }\n        }\n        return target;",
    testCases: [{ id: "tc1", input: "{}, { \"__proto__\": { \"admin\": true } }", expected: "{}", isHidden: false }]
  },

  // ==========================================
  // EXPANDED SQL DATABASE (6 New Challenges)
  // ==========================================
  {
    id: "delete-duplicate-emails",
    title: "Delete Duplicate Emails (SQL)",
    category: "SQL",
    difficulty: "Easy",
    elo: 650,
    description: "Write an SQL query to delete all duplicate email entries in a table named `Person`, keeping only unique emails with the smallest ID.",
    constraints: ["Rows must be deleted in place.", "Reference smallest ID."],
    examples: [{ input: "duplicates in Person table", output: "duplicates deleted" }],
    hints: ["Use a self-join: DELETE p1 FROM Person p1, Person p2 WHERE p1.Email = p2.Email AND p1.Id > p2.Id."],
    funcName: "deleteDuplicateEmailsQuery",
    params: "",
    tsParams: "",
    tsReturn: "string",
    pyParams: "",
    pyReturn: "str",
    javaParams: "",
    javaReturn: "String",
    jsSolution: "return 'DELETE p1 FROM Person p1 JOIN Person p2 ON p1.Email = p2.Email WHERE p1.Id > p2.Id;';",
    pySolution: "return 'DELETE p1 FROM Person p1 JOIN Person p2 ON p1.Email = p2.Email WHERE p1.Id > p2.Id'",
    javaSolution: "return \"DELETE p1 FROM Person p1 JOIN Person p2 ON p1.Email = p2.Email WHERE p1.Id > p2.Id\";",
    testCases: [{ id: "tc1", input: "Person table", expected: "passed", isHidden: false }]
  },
  {
    id: "employees-earning-more",
    title: "Employees Earning More Than Managers (SQL)",
    category: "SQL",
    difficulty: "Easy",
    elo: 600,
    description: "Write an SQL query to find the employees who earn more than their managers. The table `Employee` contains Id, Name, Salary, and ManagerId columns.",
    constraints: ["Return column labeled as 'Employee'."],
    examples: [{ input: "Employee manager matching", output: "Employee names list" }],
    hints: ["Join the Employee table with itself comparing ManagerId to employee Id."],
    funcName: "employeesEarningMoreQuery",
    params: "",
    tsParams: "",
    tsReturn: "string",
    pyParams: "",
    pyReturn: "str",
    javaParams: "",
    javaReturn: "String",
    jsSolution: "return 'SELECT e.Name AS Employee FROM Employee e JOIN Employee m ON e.ManagerId = m.Id WHERE e.Salary > m.Salary;';",
    pySolution: "return 'SELECT e.Name AS Employee FROM Employee e JOIN Employee m ON e.ManagerId = m.Id WHERE e.Salary > m.Salary'",
    javaSolution: "return \"SELECT e.Name AS Employee FROM Employee e JOIN Employee m ON e.ManagerId = m.Id WHERE e.Salary > m.Salary\";",
    testCases: [{ id: "tc1", input: "Employee table schema", expected: "passed", isHidden: false }]
  },
  {
    id: "department-highest-salary",
    title: "Department Highest Salary (SQL)",
    category: "SQL",
    difficulty: "Medium",
    elo: 1350,
    description: "Write an SQL query to find employees who have the highest salary in each of the departments.",
    constraints: ["Return columns: Department, Employee, Salary."],
    examples: [{ input: "Employee and Department schemas", output: "highest salaries list" }],
    hints: ["Use a subquery with IN or a window function DENSE_RANK()."],
    funcName: "deptHighestSalaryQuery",
    params: "",
    tsParams: "",
    tsReturn: "string",
    pyParams: "",
    pyReturn: "str",
    javaParams: "",
    javaReturn: "String",
    jsSolution: "return 'SELECT d.Name AS Department, e.Name AS Employee, e.Salary FROM Employee e JOIN Department d ON e.DepartmentId = d.Id WHERE (e.DepartmentId, e.Salary) IN (SELECT DepartmentId, MAX(Salary) FROM Employee GROUP BY DepartmentId);';",
    pySolution: "return 'SELECT d.Name AS Department, e.Name AS Employee, e.Salary FROM Employee e JOIN Department d ON e.DepartmentId = d.Id WHERE (e.DepartmentId, e.Salary) IN (SELECT DepartmentId, MAX(Salary) FROM Employee GROUP BY DepartmentId)'",
    javaSolution: "return \"SELECT d.Name AS Department, e.Name AS Employee, e.Salary FROM Employee e JOIN Department d ON e.DepartmentId = d.Id WHERE (e.DepartmentId, e.Salary) IN (SELECT DepartmentId, MAX(Salary) FROM Employee GROUP BY DepartmentId)\";",
    testCases: [{ id: "tc1", input: "Schemas", expected: "passed", isHidden: false }]
  },
  {
    id: "rank-scores",
    title: "Rank Scores (SQL)",
    category: "SQL",
    difficulty: "Medium",
    elo: 1400,
    description: "Write an SQL query to rank scores. If there is a tie between two scores, both should have the same ranking. Ranks must be consecutive integers without gaps.",
    constraints: ["Scores must be sorted in descending order.", "Use column name 'Rank'."],
    examples: [{ input: "scores: [3.5, 3.5, 4.0]", output: "ranks: [2, 2, 1]" }],
    hints: ["Use the window function DENSE_RANK() OVER (ORDER BY Score DESC)."],
    funcName: "rankScoresQuery",
    params: "",
    tsParams: "",
    tsReturn: "string",
    pyParams: "",
    pyReturn: "str",
    javaParams: "",
    javaReturn: "String",
    jsSolution: "return 'SELECT Score, DENSE_RANK() OVER (ORDER BY Score DESC) AS Rank FROM Scores;';",
    pySolution: "return 'SELECT Score, DENSE_RANK() OVER (ORDER BY Score DESC) AS Rank FROM Scores'",
    javaSolution: "return \"SELECT Score, DENSE_RANK() OVER (ORDER BY Score DESC) AS Rank FROM Scores\";",
    testCases: [{ id: "tc1", input: "Scores table schema", expected: "passed", isHidden: false }]
  },
  {
    id: "consecutive-numbers",
    title: "Consecutive Numbers (SQL)",
    category: "SQL",
    difficulty: "Medium",
    elo: 1500,
    description: "Write an SQL query to find all numbers that appear at least three times consecutively in a table named `Logs`.",
    constraints: ["Return column named 'ConsecutiveNums'."],
    examples: [{ input: "nums: 1, 1, 1, 2, 1", output: "ConsecutiveNums: 1" }],
    hints: ["Join the Logs table on itself three times where IDs are consecutive (l1.id = l2.id-1 and l2.id = l3.id-1)."],
    funcName: "consecutiveNumbersQuery",
    params: "",
    tsParams: "",
    tsReturn: "string",
    pyParams: "",
    pyReturn: "str",
    javaParams: "",
    javaReturn: "String",
    jsSolution: "return 'SELECT DISTINCT l1.Num AS ConsecutiveNums FROM Logs l1, Logs l2, Logs l3 WHERE l1.Id = l2.Id - 1 AND l2.Id = l3.Id - 1 AND l1.Num = l2.Num AND l2.Num = l3.Num;';",
    pySolution: "return 'SELECT DISTINCT l1.Num AS ConsecutiveNums FROM Logs l1, Logs l2, Logs l3 WHERE l1.Id = l2.Id - 1 AND l2.Id = l3.Id - 1 AND l1.Num = l2.Num AND l2.Num = l3.Num'",
    javaSolution: "return \"SELECT DISTINCT l1.Num AS ConsecutiveNums FROM Logs l1, Logs l2, Logs l3 WHERE l1.Id = l2.Id - 1 AND l2.Id = l3.Id - 1 AND l1.Num = l2.Num AND l2.Num = l3.Num\";",
    testCases: [{ id: "tc1", input: "Logs table schema", expected: "passed", isHidden: false }]
  },
  {
    id: "trips-and-users",
    title: "Trips and Users Cancel Rates (SQL)",
    category: "SQL",
    difficulty: "Hard",
    elo: 2000,
    description: "Write an SQL query to find the cancellation rate of requests with unbanned users (both client and driver) for each day between '2013-10-01' and '2013-10-03'.",
    constraints: ["Cancellation rate should be rounded to two decimal places.", "Format column as 'Cancellation Rate'."],
    examples: [{ input: "Trips and Users tables data", output: "dates with cancel percentage rate stats" }],
    hints: ["Filter out banned clients and drivers using WHERE clause joins.", "Compute rate: SUM(CASE WHEN Status != 'completed' THEN 1 ELSE 0 END) / COUNT(*)."],
    funcName: "tripsUsersQuery",
    params: "",
    tsParams: "",
    tsReturn: "string",
    pyParams: "",
    pyReturn: "str",
    javaParams: "",
    javaReturn: "String",
    jsSolution: "return \"SELECT Request_at AS Day, ROUND(SUM(CASE WHEN Status != 'completed' THEN 1.0 ELSE 0.0 END) / COUNT(*), 2) AS 'Cancellation Rate' FROM Trips JOIN Users c ON Client_Id = c.Users_Id JOIN Users d ON Driver_Id = d.Users_Id WHERE c.Banned = 'No' AND d.Banned = 'No' AND Request_at BETWEEN '2013-10-01' AND '2013-10-03' GROUP BY Request_at;\";",
    pySolution: "return \"SELECT Request_at AS Day, ROUND(SUM(CASE WHEN Status != 'completed' THEN 1.0 ELSE 0.0 END) / COUNT(*), 2) AS 'Cancellation Rate' FROM Trips JOIN Users c ON Client_Id = c.Users_Id JOIN Users d ON Driver_Id = d.Users_Id WHERE c.Banned = 'No' AND d.Banned = 'No' AND Request_at BETWEEN '2013-10-01' AND '2013-10-03' GROUP BY Request_at;\"",
    javaSolution: "return \"SELECT Request_at AS Day, ROUND(SUM(CASE WHEN Status != 'completed' THEN 1.0 ELSE 0.0 END) / COUNT(*), 2) AS 'Cancellation Rate' FROM Trips JOIN Users c ON Client_Id = c.Users_Id JOIN Users d ON Driver_Id = d.Users_Id WHERE c.Banned = 'No' AND d.Banned = 'No' AND Request_at BETWEEN '2013-10-01' AND '2013-10-03' GROUP BY Request_at;\";",
    testCases: [{ id: "tc1", input: "Trips table", expected: "passed", isHidden: false }]
  },

  // ==========================================
  // EXPANDED FRONTEND (7 New Challenges)
  // ==========================================
  {
    id: "react-todo-list",
    title: "React Todo List Manager",
    category: "Frontend",
    difficulty: "Easy",
    elo: 600,
    description: "Build a React component `TodoList` that maintains state for lists, adds items, and removes specific list nodes by list ID index.",
    constraints: ["Input text must update state.", "Adding item must clear input.", "Must handle delete actions."],
    examples: [{ input: "Type 'buy milk', press add", output: "milk displays, input empties" }],
    hints: ["Use local state Hook for input string and items array.", "Map array matching element ID indices in list renders."],
    funcName: "TodoList",
    params: "",
    tsParams: "",
    tsReturn: "any",
    pyParams: "",
    pyReturn: "any",
    javaParams: "",
    javaReturn: "any",
    jsSolution: "const [todos, setTodos] = React.useState([]);\n  const [val, setVal] = React.useState('');\n  const add = () => {\n    if (val.trim()) { setTodos([...todos, val]); setVal(''); }\n  };\n  const del = (idx) => {\n    setTodos(todos.filter((_, i) => i !== idx));\n  };\n  return React.createElement('div', null,\n    React.createElement('input', { value: val, onChange: (e) => setVal(e.target.value) }),\n    React.createElement('button', { onClick: add }, 'Add'),\n    React.createElement('ul', null, todos.map((t, i) =>\n      React.createElement('li', { key: i }, t, React.createElement('button', { onClick: () => del(i) }, 'X'))\n    ))\n  );",
    pySolution: "class TodoList:\n    def __init__(self): self.todos = []",
    javaSolution: "public class TodoList { }",
    testCases: [{ id: "tc1", input: "render TodoList", expected: "passed", isHidden: false }]
  },
  {
    id: "react-debounce-input",
    title: "React Debounced Input Hook",
    category: "Frontend",
    difficulty: "Medium",
    elo: 1350,
    description: "Write a React custom hook `useDebounce(value, delay)` that holds updates and outputs input variables only after the specified delay timeout completes.",
    constraints: ["Hook must handle dynamic dependencies.", "Should clear timeout locks cleanly on unmount."],
    examples: [{ input: "text typed with 500ms delay", output: "state updates after pause" }],
    hints: ["Use React.useEffect.", "Declare setTimeout inside, and clear timeout in destructor callback."],
    funcName: "useDebounce",
    params: "value, delay",
    tsParams: "value: any, delay: number",
    tsReturn: "any",
    pyParams: "value: any, delay: int",
    pyReturn: "any",
    javaParams: "Object value, int delay",
    javaReturn: "Object",
    jsSolution: "const [debounced, setDebounced] = React.useState(value);\n  React.useEffect(() => {\n    const handler = setTimeout(() => setDebounced(value), delay);\n    return () => clearTimeout(handler);\n  }, [value, delay]);\n  return debounced;",
    pySolution: "pass # Custom react hooks mock",
    javaSolution: "public class Debounce { }",
    testCases: [{ id: "tc1", input: "debounce inputs", expected: "passed", isHidden: false }]
  },
  {
    id: "react-infinite-scroll",
    title: "React Infinite Scroll Observer",
    category: "Frontend",
    difficulty: "Medium",
    elo: 1500,
    description: "Create a react component `InfiniteScroll` that monitors browser scrolls and triggers load callbacks when footer elements become visible in viewports.",
    constraints: ["Use IntersectionObserver APIs.", "Must disconnect locks dynamically on cleanup."],
    examples: [{ input: "User scrolls down", output: "trigger next page load" }],
    hints: ["Use React.useRef to track DOM elements.", "Instantiate observer in useEffect referencing target wrapper ref."],
    funcName: "InfiniteScroll",
    params: "onLoadMore",
    tsParams: "onLoadMore: () => void",
    tsReturn: "any",
    pyParams: "onload_more: any",
    pyReturn: "any",
    javaParams: "Object onLoadMore",
    javaReturn: "Object",
    jsSolution: "const triggerRef = React.useRef(null);\n  React.useEffect(() => {\n    const obs = new IntersectionObserver((entries) => {\n      if (entries[0].isIntersecting) onLoadMore();\n    });\n    if (triggerRef.current) obs.observe(triggerRef.current);\n    return () => obs.disconnect();\n  }, [onLoadMore]);\n  return React.createElement('div', { ref: triggerRef }, 'Loading more...');",
    pySolution: "pass",
    javaSolution: "public class InfiniteScroll { }",
    testCases: [{ id: "tc1", input: "scroll check", expected: "passed", isHidden: false }]
  },
  {
    id: "react-form-validation",
    title: "React Signup Form Validator",
    category: "Frontend",
    difficulty: "Easy",
    elo: 700,
    description: "Build a signup form component `SignupForm` validating fields: Email must contain '@', password length must exceed 6, and password matching field must match.",
    constraints: ["Errors must show in a container with id 'error-msg'.", "Disable submit button if inputs are empty or invalid."],
    examples: [{ input: "Type wrong password match, submit", output: "shows match error message" }],
    hints: ["Sync state for each input field.", "Compute validations inline, rendering error alerts conditionally."],
    funcName: "SignupForm",
    params: "",
    tsParams: "",
    tsReturn: "any",
    pyParams: "",
    pyReturn: "any",
    javaParams: "",
    javaReturn: "any",
    jsSolution: "const [form, setForm] = React.useState({ email: '', pass: '', confirm: '' });\n  const error = !form.email.includes('@') ? 'Invalid email' :\n                form.pass.length < 6 ? 'Password too short' :\n                form.pass !== form.confirm ? 'Passwords mismatch' : '';\n  return React.createElement('form', null,\n    React.createElement('input', { value: form.email, onChange: e => setForm({ ...form, email: e.target.value }) }),\n    React.createElement('input', { type: 'password', value: form.pass, onChange: e => setForm({ ...form, pass: e.target.value }) }),\n    React.createElement('input', { type: 'password', value: form.confirm, onChange: e => setForm({ ...form, confirm: e.target.value }) }),\n    error && React.createElement('div', { id: 'error-msg' }, error),\n    React.createElement('button', { disabled: !!error }, 'Submit')\n  );",
    pySolution: "pass",
    javaSolution: "public class SignupForm { }",
    testCases: [{ id: "tc1", input: "form validation", expected: "passed", isHidden: false }]
  },
  {
    id: "react-tabs-component",
    title: "React Navigation Tab Switcher",
    category: "Frontend",
    difficulty: "Easy",
    elo: 550,
    description: "Build a react switcher `Tabs` component that manages multiple view states, highlighting active tabs and rendering target container panels dynamically.",
    constraints: ["Manage active index via local state.", "Clicking tab shifts view selection."],
    examples: [{ input: "Click tab index 2", output: "shows child panel content 2" }],
    hints: ["Define tabs metadata array.", "Render header buttons mapped to state index togglers."],
    funcName: "Tabs",
    params: "config",
    tsParams: "config: any[]",
    tsReturn: "any",
    pyParams: "config: list",
    pyReturn: "any",
    javaParams: "List<Object> config",
    javaReturn: "Object",
    jsSolution: "const [active, setActive] = React.useState(0);\n  return React.createElement('div', null,\n    React.createElement('div', { className: 'headers' }, config.map((tab, i) =>\n      React.createElement('button', { key: i, onClick: () => setActive(i), className: active === i ? 'active' : '' }, tab.title)\n    )),\n    React.createElement('div', { className: 'panel' }, config[active]?.content || null)\n  );",
    pySolution: "pass",
    javaSolution: "public class Tabs { }",
    testCases: [{ id: "tc1", input: "render tab configs", expected: "passed", isHidden: false }]
  },
  {
    id: "react-context-darkmode",
    title: "React Global Context Theme Provider",
    category: "Frontend",
    difficulty: "Medium",
    elo: 1300,
    description: "Create a React context wrapper `ThemeProvider` exposing `theme` (light/dark) and `toggleTheme` capabilities to deeply nested child views.",
    constraints: ["Expose value via Context.Provider.", "Ensure nested child hooks consume contexts cleanly."],
    examples: [{ input: "Wrap children in Provider", output: "Deep consumers toggle themes properly" }],
    hints: ["Use React.createContext() and React.useContext()."],
    funcName: "ThemeProvider",
    params: "children",
    tsParams: "children: any",
    tsReturn: "any",
    pyParams: "children: any",
    pyReturn: "any",
    javaParams: "Object children",
    javaReturn: "Object",
    jsSolution: "const ThemeContext = React.createContext();\n  const [theme, setTheme] = React.useState('dark');\n  const toggle = () => setTheme(prev => prev === 'light' ? 'dark' : 'light');\n  return React.createElement(ThemeContext.Provider, { value: { theme, toggleTheme: toggle } }, children);",
    pySolution: "pass",
    javaSolution: "public class ThemeProvider { }",
    testCases: [{ id: "tc1", input: "context wrapping", expected: "passed", isHidden: false }]
  },
  {
    id: "react-modal-dialog",
    title: "React Modal Backdrop Dialog",
    category: "Frontend",
    difficulty: "Easy",
    elo: 600,
    description: "Design an overlay component `Modal` displaying dialog content. Clicking backdrop bounds or pressing the 'Escape' key must dismiss modal views.",
    constraints: ["Attach escape key listener to document global lifecycle.", "Remove global event listeners on unmounting phase."],
    examples: [{ input: "Press escape key", output: "modal closes" }],
    hints: ["Use React.useEffect to bind keydown window listeners.", "Remember to return callback listeners destructor."],
    funcName: "Modal",
    params: "isOpen, onClose, content",
    tsParams: "isOpen: boolean, onClose: () => void, content: any",
    tsReturn: "any",
    pyParams: "is_open: bool, on_close: any, content: any",
    pyReturn: "any",
    javaParams: "boolean isOpen, Object onClose, Object content",
    javaReturn: "Object",
    jsSolution: "React.useEffect(() => {\n    const handleEscape = (e) => { if (e.key === 'Escape') onClose(); };\n    if (isOpen) window.addEventListener('keydown', handleEscape);\n    return () => window.removeEventListener('keydown', handleEscape);\n  }, [isOpen, onClose]);\n  if (!isOpen) return null;\n  return React.createElement('div', { className: 'modal-backdrop', onClick: onClose },\n    React.createElement('div', { className: 'modal-content', onClick: e => e.stopPropagation() }, content)\n  );",
    pySolution: "pass",
    javaSolution: "public class Modal { }",
    testCases: [{ id: "tc1", input: "modal overlays", expected: "passed", isHidden: false }]
  },

  // ==========================================
  // EXPANDED SYSTEM DESIGN / APIs (7 New Challenges)
  // ==========================================
  {
    id: "snowflake-id-generator",
    title: "Snowflake Distributed Unique ID Generator",
    category: "System Design",
    difficulty: "Hard",
    elo: 2100,
    description: "Implement a Snowflake-style unique ID generator. Given timestamp milliseconds, worker ID, data center ID, and sequences, compose a 64-bit ID string.",
    constraints: ["Bit arrangement: sign bit (1 bit), timestamp (41 bits), datacenter (5 bits), worker (5 bits), sequence (12 bits).", "Throw exceptions if parameters exceed boundary capacity."],
    examples: [{ input: "timestamp = 1600000000000, datacenter = 1, worker = 1, sequence = 0", output: "unique binary-packed ID" }],
    hints: ["Use JavaScript BigInt for bitwise shifting operations.", "Datacenter ID and worker ID are bounded by 31 (5 bits)."],
    funcName: "generateSnowflakeId",
    params: "timestamp, datacenterId, workerId, sequence",
    tsParams: "timestamp: number, datacenterId: number, workerId: number, sequence: number",
    tsReturn: "string",
    pyParams: "timestamp: int, datacenter_id: int, worker_id: int, sequence: int",
    pyReturn: "str",
    javaParams: "long timestamp, long datacenterId, long workerId, long sequence",
    javaReturn: "String",
    jsSolution: "const epoch = 1288834974657n;\n  const ts = BigInt(timestamp) - epoch;\n  const dc = BigInt(datacenterId) & 31n;\n  const wk = BigInt(workerId) & 31n;\n  const seq = BigInt(sequence) & 4095n;\n  const id = (ts << 22n) | (dc << 17n) | (wk << 12n) | seq;\n  return id.toString();",
    pySolution: "epoch = 1288834974657\n    ts = timestamp - epoch\n    dc = datacenter_id & 31\n    wk = worker_id & 31\n    seq = sequence & 4095\n    return str((ts << 22) | (dc << 17) | (wk << 12) | seq)",
    javaSolution: "long epoch = 1288834974657L;\n        long ts = timestamp - epoch;\n        long dc = datacenterId & 31L;\n        long wk = workerId & 31L;\n        long seq = sequence & 4095L;\n        long id = (ts << 22) | (dc << 17) | (wk << 12) | seq;\n        return String.valueOf(id);",
    testCases: [{ id: "tc1", input: "1600000000000, 1, 1, 0", expected: "1388650253316100096", isHidden: false }]
  },
  {
    id: "pub-sub-broker",
    title: "Pub-Sub Broker Wildcard Router",
    category: "System Design",
    difficulty: "Medium",
    elo: 1500,
    description: "Write a message broker tracking event paths. Support subscription hooks, message routing, and wildcard tokens matching nested paths (e.g. `order.*` or `user.created`).",
    constraints: ["Subscribing yields unsubscribe handles.", "Wildcard matches dynamic namespaces."],
    examples: [{ input: "subscribe('orders.*'), publish('orders.created', data)", output: "triggers subscription callback" }],
    hints: ["Maintain map linking paths to arrays of hook delegates.", "Evaluate topics matching routing patterns using string splits."],
    funcName: "PubSubBroker",
    params: "",
    tsParams: "",
    tsReturn: "any",
    pyParams: "",
    pyReturn: "any",
    javaParams: "",
    javaReturn: "any",
    jsSolution: "class PubSub {\n    constructor() { this.subs = new Map(); }\n    sub(topic, fn) {\n      if (!this.subs.has(topic)) this.subs.set(topic, []);\n      this.subs.get(topic).push(fn);\n      return () => { this.subs.set(topic, this.subs.get(topic).filter(x => x !== fn)); };\n    }\n    pub(topic, data) {\n      for (const [key, fns] of this.subs.entries()) {\n        const pattern = new RegExp('^' + key.replace('.', '\\\\.').replace('*', '.*') + '$');\n        if (pattern.test(topic)) fns.forEach(fn => fn(data));\n      }\n    }\n  }\n  return new PubSub();",
    pySolution: "import re\nclass PubSub:\n    def __init__(self): self.subs = {}\n    def sub(self, topic, fn):\n        self.subs.setdefault(topic, []).append(fn)\n        return lambda: self.subs[topic].remove(fn)\n    def pub(self, topic, data):\n        for key, fns in self.subs.items():\n            pattern = '^' + key.replace('.', '\\\\.').replace('*', '.*') + '$'\n            if re.match(pattern, topic):\n                for fn in fns: fn(data)",
    javaSolution: "public class PubSubBroker { }",
    testCases: [{ id: "tc1", input: "publish wildcards", expected: "passed", isHidden: false }]
  },
  {
    id: "event-sourcing-store",
    title: "Event Sourcing State Reconstructor",
    category: "System Design",
    difficulty: "Hard",
    elo: 2050,
    description: "Given an initial state object and an array of append-only events (e.g. `{ type: 'ADD_ITEM', payload: { id: 1 } }`), apply transition events in sequence to reconstruct the final aggregate state.",
    constraints: ["Aggregate operations must be side-effect free.", "Unrecognized events should preserve state configurations."],
    examples: [{ input: "init = { count: 0 }, events = [{ type: 'ADD' }, { type: 'ADD' }]", output: "{ count: 2 }" }],
    hints: ["Use reduction operators (reducers) tracking action payloads."],
    funcName: "reconstructState",
    params: "initialState, events",
    tsParams: "initialState: any, events: any[]",
    tsReturn: "any",
    pyParams: "initial_state: dict, events: list[dict]",
    pyReturn: "dict",
    javaParams: "Map<String, Object> initialState, List<Map<String, Object>> events",
    javaReturn: "Map<String, Object>",
    jsSolution: "return events.reduce((state, event) => {\n    const next = { ...state };\n    if (event.type === 'INCREMENT') next.count = (next.count || 0) + (event.val || 1);\n    if (event.type === 'DECREMENT') next.count = (next.count || 0) - (event.val || 1);\n    return next;\n  }, initialState);",
    pySolution: "state = dict(initial_state)\n    for event in events:\n        t = event.get('type')\n        if t == 'INCREMENT': state['count'] = state.get('count', 0) + event.get('val', 1)\n        elif t == 'DECREMENT': state['count'] = state.get('count', 0) - event.get('val', 1)\n    return state",
    javaSolution: "java.util.Map<String, Object> state = new java.util.HashMap<>(initialState);\n        for (java.util.Map<String, Object> e : events) {\n            String t = (String) e.get(\"type\");\n            int val = e.containsKey(\"val\") ? ((Number) e.get(\"val\")).intValue() : 1;\n            int curr = ((Number) state.getOrDefault(\"count\", 0)).intValue();\n            if (\"INCREMENT\".equals(t)) state.put(\"count\", curr + val);\n            else if (\"DECREMENT\".equals(t)) state.put(\"count\", curr - val);\n        }\n        return state;",
    testCases: [{ id: "tc1", input: "{ \"count\": 0 }, [{ \"type\": \"INCREMENT\", \"val\": 5 }]", expected: "{ \"count\": 5 }", isHidden: false }]
  },
  {
    id: "consistent-hashing-ring",
    title: "Consistent Hashing Ring Coordinator",
    category: "System Design",
    difficulty: "Hard",
    elo: 2200,
    description: "Design a consistent hashing ring. Implement methods to add server nodes, remove nodes, and resolve client keys to the nearest corresponding node on the hashing circle.",
    constraints: ["Hashing ring must support virtual nodes.", "Ensure balanced key distribution across servers."],
    examples: [{ input: "addNode('ServerA'), getPrimaryNode('ClientKey')", output: "'ServerA'" }],
    hints: ["Use a hashing function (like MD5 or SHA-1) to map both nodes and keys to an integer value on a circle [0, 2^32 - 1]."],
    funcName: "ConsistentHashRing",
    params: "replicas",
    tsParams: "replicas: number",
    tsReturn: "any",
    pyParams: "replicas: int",
    pyReturn: "any",
    javaParams: "int replicas",
    javaReturn: "Object",
    jsSolution: "class HashRing {\n    constructor(replicas) {\n      this.replicas = replicas;\n      this.ring = [];\n      this.nodeMap = new Map();\n    }\n    add(node) {\n      for (let i = 0; i < this.replicas; i++) {\n        const hash = this.hash(node + '-' + i);\n        this.ring.push(hash);\n        this.nodeMap.set(hash, node);\n      }\n      this.ring.sort((a, b) => a - b);\n    }\n    remove(node) {\n      for (let i = 0; i < this.replicas; i++) {\n        const hash = this.hash(node + '-' + i);\n        this.ring = this.ring.filter(x => x !== hash);\n        this.nodeMap.delete(hash);\n      }\n    }\n    get(key) {\n      if (!this.ring.length) return null;\n      const hash = this.hash(key);\n      const idx = this.ring.findIndex(x => x >= hash);\n      return this.nodeMap.get(this.ring[idx === -1 ? 0 : idx]);\n    }\n    hash(str) {\n      let h = 0;\n      for (let i = 0; i < str.length; i++) {\n        h = (Math.imul(31, h) + str.charCodeAt(i)) | 0;\n      }\n      return Math.abs(h);\n    }\n  }\n  return new HashRing(replicas);",
    pySolution: "class HashRing:\n    def __init__(self, replicas):\n        self.replicas = replicas\n        self.ring = []\n        self.node_map = {}\n    def add(self, node):\n        for i in range(self.replicas):\n            h = self._hash(f\"{node}-{i}\")\n            self.ring.append(h)\n            self.node_map[h] = node\n        self.ring.sort()\n    def remove(self, node):\n        for i in range(self.replicas):\n            h = self._hash(f\"{node}-{i}\")\n            if h in self.ring: self.ring.remove(h)\n            self.node_map.pop(h, None)\n    def get(self, key):\n        if not self.ring: return None\n        h = self._hash(key)\n        for val in self.ring:\n            if val >= h: return self.node_map[val]\n        return self.node_map[self.ring[0]]\n    def _hash(self, val):\n        return abs(hash(val))",
    javaSolution: "public class HashRing { }",
    testCases: [{ id: "tc1", input: "hash ring lookups", expected: "passed", isHidden: false }]
  },
  {
    id: "graphql-schema-parser",
    title: "GraphQL Schema Token Parser",
    category: "System Design",
    difficulty: "Medium",
    elo: 1450,
    description: "Write a parser function `parseGraphQLSchema(schemaText)` that extracts types, field names, and returns type declarations from raw GraphQL text payloads.",
    constraints: ["Parse curly braces and fields correctly.", "Ignore whitespace and comments."],
    examples: [{ input: "schemaText = 'type User { id: ID }'", output: "{ User: { id: 'ID' } }" }],
    hints: ["Use regex token matchers or recursive string splitting lines."],
    funcName: "parseGraphQLSchema",
    params: "schemaText",
    tsParams: "schemaText: string",
    tsReturn: "any",
    pyParams: "schema_text: str",
    pyReturn: "any",
    javaParams: "String schemaText",
    javaReturn: "Object",
    jsSolution: "const result = {};\n  const lines = schemaText.split('\\n');\n  let currType = null;\n  for (const line of lines) {\n    const trim = line.trim();\n    if (trim.startsWith('type ')) {\n      currType = trim.split(' ')[1];\n      result[currType] = {};\n    } else if (trim.includes(':') && currType) {\n      const [field, type] = trim.split(':').map(s => s.trim());\n      result[currType][field] = type.replace('!', '');\n    }\n  }\n  return result;",
    pySolution: "res = {}\n    curr_type = None\n    for line in schema_text.split('\\n'):\n        trim = line.trim()\n        if trim.startswith('type '):\n            curr_type = trim.split(' ')[1]\n            res[curr_type] = {}\n        elif ':' in trim and curr_type:\n            parts = [p.strip() for p in trim.split(':')]\n            res[curr_type][parts[0]] = parts[1].replace('!', '')\n    return res",
    javaSolution: "public class SchemaParser { }",
    testCases: [{ id: "tc1", input: "\"type User { id: ID }\"", expected: "{\n  \"User\": {\n    \"id\": \"ID\"\n  }\n}", isHidden: false }]
  },
  {
    id: "resilient-load-balancer",
    title: "Resilient Health-Checking Load Balancer",
    category: "System Design",
    difficulty: "Hard",
    elo: 2150,
    description: "Implement a weighted round-robin load balancer. The router distributes client requests to worker nodes based on capacity weights, while bypassing unhealthy workers.",
    constraints: ["Dynamically update worker health states.", "Fallback gracefully if all backend nodes are unhealthy."],
    examples: [{ input: "nodes = { A: { weight: 2, healthy: true } }, request()", output: "'A'" }],
    hints: ["Maintain index cursor counters.", "Filter active servers by health key and evaluate weights recursively."],
    funcName: "LoadBalancer",
    params: "nodes",
    tsParams: "nodes: any[]",
    tsReturn: "any",
    pyParams: "nodes: list",
    pyReturn: "any",
    javaParams: "List<Object> nodes",
    javaReturn: "Object",
    jsSolution: "class LB {\n    constructor(nodes) { this.nodes = nodes; this.idx = 0; }\n    next() {\n      const active = this.nodes.filter(n => n.healthy);\n      if (!active.length) return null;\n      const target = active[this.idx % active.length];\n      this.idx++;\n      return target.id;\n    }\n  }\n  return new LB(nodes);",
    pySolution: "class LB:\n    def __init__(self, nodes):\n        self.nodes = nodes\n        self.idx = 0\n    def next(self):\n        active = [n for n in self.nodes if n.get('healthy')]\n        if not active: return None\n        target = active[self.idx % len(active)]\n        self.idx += 1\n        return target.get('id')",
    javaSolution: "public class LoadBalancer { }",
    testCases: [{ id: "tc1", input: "request router balance", expected: "passed", isHidden: false }]
  },
  {
    id: "database-transaction-retry",
    title: "Database Retry Loops With Jitter",
    category: "System Design",
    difficulty: "Medium",
    elo: 1400,
    description: "Write a resilient runner function `runTransaction(operation, retries, backoffMs)` executing callbacks, backing off exponentially, and applying randomized jitter when databases block writes.",
    constraints: ["Retry on transaction exceptions.", "Incorporate randomized milliseconds of jitter to avoid concurrency stampedes."],
    examples: [{ input: "failing db operation", output: "success after 2 retries" }],
    hints: ["Use sleep promises inside helper retry loops.", "Math.random() scaling randomizes backoff pauses dynamically."],
    funcName: "runTransaction",
    params: "operation, retries, backoffMs",
    tsParams: "operation: any, retries: number, backoffMs: number",
    tsReturn: "Promise<any>",
    pyParams: "operation: any, retries: int, backoff_ms: int",
    pyReturn: "any",
    javaParams: "Object operation, int retries, int backoffMs",
    javaReturn: "Object",
    jsSolution: "for (let i = 0; i <= retries; i++) {\n    try {\n      return await operation();\n    } catch (e) {\n      if (i === retries) throw e;\n      const jitter = Math.random() * 100;\n      const sleep = backoffMs * Math.pow(2, i) + jitter;\n      await new Promise(r => setTimeout(r, sleep));\n    }\n  }",
    pySolution: "import time, random\n    for i in range(retries + 1):\n        try:\n            return operation()\n        except Exception as e:\n            if i == retries: raise e\n            sleep_time = (backoff_ms * (2 ** i) + random.uniform(0, 100)) / 1000.0\n            time.sleep(sleep_time)",
    javaSolution: "public class TransactionExecutor { }",
    testCases: [{ id: "tc1", input: "transaction loops", expected: "passed", isHidden: false }]
  }
];

export const getProceduralChallenges = (): ChallengeDefinition[] => {
  return PROCEDURAL_SCHEMAS.map(s => {
    // Generate JS / TS / PY / JAVA templates
    const templates: Record<string, { starterCode: string; solutionTemplate: string; testCases: TestCase[] }> = {};

    // JavaScript
    templates["JavaScript"] = {
      starterCode: `function ${s.funcName}(${s.params}) {\n  // TODO: Implement\n  return null;\n}`,
      solutionTemplate: `function ${s.funcName}(${s.params}) {\n  ${s.jsSolution.split("\n").join("\n  ")}\n}`,
      testCases: s.testCases
    };

    // TypeScript
    templates["TypeScript"] = {
      starterCode: `export function ${s.funcName}(${s.tsParams}): ${s.tsReturn} {\n  // TODO: Implement\n  return null as any;\n}`,
      solutionTemplate: `export function ${s.funcName}(${s.tsParams}): ${s.tsReturn} {\n  ${s.jsSolution.split("\n").join("\n  ")}\n}`,
      testCases: s.testCases
    };

    // Python
    const pyFuncName = s.funcName.replace(/[A-Z]/g, letter => "_" + letter.toLowerCase());
    templates["Python"] = {
      starterCode: `def ${pyFuncName}(${s.pyParams}) -> ${s.pyReturn}:\n    # TODO: Implement\n    pass`,
      solutionTemplate: `def ${pyFuncName}(${s.pyParams}) -> ${s.pyReturn}:\n    ${s.pySolution.split("\n").join("\n    ")}`,
      testCases: s.testCases.map(tc => ({
        ...tc,
        expected: tc.expected === "true" ? "True" : tc.expected === "false" ? "False" : tc.expected
      }))
    };

    // Java
    templates["Java"] = {
      starterCode: `public class Solution {\n    public ${s.javaReturn} ${s.funcName}(${s.javaParams}) {\n        // TODO: Implement\n        return null;\n    }\n}`,
      solutionTemplate: `public class Solution {\n    public ${s.javaReturn} ${s.funcName}(${s.javaParams}) {\n        ${s.javaSolution.split("\n").join("\n        ")}\n    }\n}`,
      testCases: s.testCases
    };

    return {
      id: s.id,
      title: s.title,
      category: s.category,
      difficulty: s.difficulty,
      elo: s.elo,
      description: s.description,
      constraints: s.constraints,
      examples: s.examples,
      hints: s.hints,
      templates
    };
  });
};
